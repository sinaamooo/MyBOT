# 🤖 NIKTO CRYPTO BOT

ربات تلگرامی PHP برای انتشار خودکار کارت‌های گرافیکی بازار رمزارز در کانال.

پنج کارت آماده دارد و همه‌چیز از داخل خود تلگرام مدیریت می‌شود — بدون نیاز به ویرایش کد:

| کارت | محتوا | منبع داده |
|---|---|---|
| 📈 **نوسان ارزها** | قیمت، درصد و مقدار تغییر ۲۴ ساعته، نمودار کوچک ۲۴ ساعته برای تا ۹ ارز | Binance → CoinGecko |
| 😨 **ترس و طمع** | سنجه‌ی افقی قرمز تا سبز، مقدار دیروز / ۷ روز / ۱ ماه پیش، نمودار ۳۰ روزه | alternative.me |
| 📅 **تقویم اقتصادی** | جدول رویدادهای روز با زمان، ارز، پرچم، اهمیت، مقدار قبلی و پیش‌بینی — **ترجمه‌شده به فارسی** | ForexFactory → TradingView |
| 🚀 **برترین‌های فیوچرز** | ۵ ارز با بیشترین رشد و ۵ ارز با بیشترین افت ۲۴ ساعته، با قیمت، حجم، نمودار کوچک و وضعیت کل بازار | Binance Futures (رایگان، بدون کلید) |
| 💧 **نقدینگی بیت‌کوین** | دیوارهای پرحجم خرید و فروش روی نمودار ۴۸ ساعته، عمق دفتر سفارش و توازن خرید/فروش | دفتر سفارش Binance فیوچرز + اسپات (رایگان) |

کارت فیوچرز فقط قراردادهای دائمی فعال USDT را حساب می‌کند (قراردادهای حذف‌شده، سررسیددار و USDC کنار می‌روند)
و ارزهایی که حجم ۲۴ ساعته‌شان از حد تعیین‌شده کمتر است در رتبه‌بندی نمی‌آیند.
کارت نقدینگی سفارش‌ها را در بازه‌های قیمتی جمع می‌کند و فقط سطح‌هایی را نشان می‌دهد که دست‌کم ۱٫۵ برابر
سطح معمول حجم دارند.

---

## ✨ ویژگی‌ها

- **طراحی روشن و خوانا** — پس‌زمینه‌ی سفید با تأکید سبز و قرمز و حاشیه‌های مشکی؛ در حالت روشن و تیره‌ی تلگرام یکسان دیده می‌شود.
- **لوگوی اختصاصی هر ارز** روی کارت قیمت‌ها (۴۱ لوگو همراه پروژه، بدون نیاز به اینترنت).
- **پنل کامل فارسی داخل تلگرام** — کانال مقصد، ساعت ارسال، روزهای هفته، تم رنگی، کپشن و ارزها؛ همه با دکمه.
- **زمان‌بندی دقیقه‌ای** — برای هر کارت چند ساعت مختلف در روز، با انتخاب روزهای هفته و منطقه‌ی زمانی دلخواه (پیش‌فرض `Asia/Tehran`).
- **تاریخ شمسی** روی کارت‌ها و در پنل.
- **متن فارسی واقعی روی تصویر** — موتور اختصاصی شکل‌دهی حروف (اتصال حروف، لام‌الف، نیم‌فاصله) و چینش دوسویه (Bidi) که اعداد و متن لاتین را در جای درست نگه می‌دارد.
- **۴ تم روشن** — «روشن» (سفید/سبز/قرمز، پیش‌فرض)، برفی، نعنایی و کاغذی.
- **پرچم‌های برداری** ۲۵ کشور — بدون نیاز به فایل تصویر یا فونت ایموجی.
- **بدون وابستگی خارجی** — نه Composer، نه کتابخانه‌ی جانبی. فقط PHP + GD + SQLite.
- **جلوگیری از ارسال تکراری** و **جبران تأخیر** اگر سرور چند دقیقه خواب بود.
- **تست خودکار آفلاین** (`tools/selftest.php`) و **بازرس چیدمان** (`tools/layout-check.php`) که وسط‌چین بودن هر عدد، خط پایه‌ی مشترک متن‌های هم‌ردیف و فاصله‌ی عنوان و عدد را با دقت زیر یک پیکسل می‌سنجد.

---

## ⚡️ نصب

### پیش‌نیازها

```bash
# دبیان / اوبونتو
sudo apt update
sudo apt install -y php-cli php-gd php-curl php-sqlite3 php-mbstring
```

PHP نسخه‌ی **۸.۱ یا بالاتر** لازم است و افزونه‌ی GD باید با پشتیبانی FreeType ساخته شده باشد.

### گام‌های نصب

```bash
git clone https://github.com/sinaamooo/MyBOT.git
cd MyBOT

# ثبت توکن (فقط یک‌بار) — فایل config.local.php ساخته می‌شود
php tools/install.php --token=123456789:AAH...

# بررسی سلامت نصب
php tools/doctor.php

# اجرا
php bot.php          # ربات و پنل
php scheduler.php    # زمان‌بند (در ترمینال دوم)
```

تنظیمات عمومی (شناسه‌ی مدیر `6595849261`، دامنه‌ی وب‌هوک، منطقه‌ی زمانی) از قبل در
`config.php` آماده است؛ فقط توکن باید یک‌بار ثبت شود.

> 🔐 **چرا توکن در مخزن نیست؟** هر کسی که به مخزن دسترسی داشته باشد با آن توکن
> کنترل کامل ربات را می‌گیرد. به همین دلیل توکن در `config.local.php` ذخیره می‌شود
> که در `.gitignore` است. راه دیگر: `BOT_TOKEN=123456789:AAH... php bot.php`
>
> اگر توکن جایی لو رفت، در @BotFather دستور `/revoke` را بزنید و دوباره
> `php tools/install.php --token=... --force` را اجرا کنید.

---

## 🚀 راه‌اندازی در ۴ قدم

1. ربات را در کانال مقصد **ادمین** کنید (با دسترسی ارسال پیام).
2. در چت خصوصی ربات `/start` بزنید ← **📡 کانال‌ها ← ➕ افزودن کانال** ← یک پست از کانال را فوروارد کنید (یا `@username` کانال را بفرستید).
3. وارد هر کارت شوید ← **⏰ زمان‌بندی ← ➕ افزودن زمان** و ساعت‌ها را بفرستید، مثلاً:
   ```
   09:00, 15:30, 21:45
   ```
   در صورت نیاز با **📆 روزها** فقط روزهای خاصی از هفته را انتخاب کنید.
4. همان کارت را با **✅ روشن کن** فعال کنید. تمام.

با **👁 پیش‌نمایش** کارت فقط برای خودتان ساخته و ارسال می‌شود و با **🚀 ارسال فوری** بلافاصله به کانال‌ها می‌رود.

---

## 🧭 نقشه‌ی پنل

```
🤖 پنل مدیریت
├── 📈 کارت نوسان ارزها
│   ├── ✅ روشن/خاموش            ├── 🎨 تم کارت (۴ تم)
│   ├── 📡 کانال‌های مقصد         ├── ⏰ زمان‌بندی ← ➕ افزودن زمان / 📆 روزها
│   ├── 📝 کپشن                  ├── 🔧 تنظیمات کارت (ارزها، عنوان، نمودار)
│   └── 👁 پیش‌نمایش              └── 🚀 ارسال فوری
├── 😨 شاخص ترس و طمع            (همان گزینه‌ها)
├── 📅 تقویم اقتصادی             (+ انتخاب امروز/فردا و حداقل اهمیت)
├── 📡 کانال‌ها                   افزودن، حذف، بررسی دسترسی ربات
├── ⚙️ تنظیمات                   منطقه‌ی زمانی، نام برند، ارقام فارسی/لاتین،
│                                کیفیت تصویر، منبع داده، ارزهای تقویم، مدیران
├── 📊 وضعیت                     ارسال بعدی هر کارت و ضربان زمان‌بند
└── 🧾 گزارش‌ها                   آخرین اجراها و خطاها
```

### دستورها

| دستور | کار |
|---|---|
| `/start` یا `/panel` | باز کردن پنل |
| `/status` | وضعیت سامانه |
| `/send prices` | ارسال فوری یک کارت (`prices` \| `feargreed` \| `calendar` \| `movers` \| `liquidity`) |
| `/id` | دیدن شناسه‌ی عددی خودتان |
| `/cancel` | لغو ورودی در حال انتظار |

---

## 🔧 اجرای دائمی (systemd)

```bash
sudo cp deploy/nikto-bot.service deploy/nikto-scheduler.service /etc/systemd/system/
sudo nano /etc/systemd/system/nikto-bot.service     # مسیر و کاربر را اصلاح کنید
sudo systemctl daemon-reload
sudo systemctl enable --now nikto-bot nikto-scheduler
sudo systemctl status nikto-bot
```

اگر systemd ندارید، زمان‌بند را با cron اجرا کنید:

```cron
* * * * * /usr/bin/php /path/to/MyBOT/scheduler.php --once >> /dev/null 2>&1
```

### حالت وب‌هوک (به‌جای `bot.php`)

آدرس وب‌هوک در `config.php` تنظیم شده است:

```
https://nikto.s14.telviprobot.top/webhook.php
```

```bash
php tools/webhook-set.php            # ثبت همین آدرس روی تلگرام
php tools/webhook-set.php --info     # وضعیت فعلی
php tools/webhook-set.php --delete   # حذف وب‌هوک و برگشت به bot.php
```

پیش‌نیازها:

1. دامنه باید **SSL معتبر** داشته باشد (تلگرام فقط `https` می‌پذیرد).
2. ریشه‌ی دامنه به پوشه‌ی پروژه اشاره کند؛ نمونه‌ی پیکربندی در `deploy/nginx.conf.sample`
   و برای هاست اشتراکی `deploy/.htaccess.sample`.
   با این تنظیم، هم `/` و هم `/webhook.php` آپدیت‌ها را می‌گیرند.
3. مسیرهای `config.php`، `data/` و `storage/` نباید از بیرون قابل دسترسی باشند —
   هر دو فایل نمونه این کار را انجام می‌دهند.
4. در حالت وب‌هوک `bot.php` لازم نیست، اما **`scheduler.php` باید همچنان اجرا شود**.

---

## 📦 نسخه‌ی «دو فایلی» (هاست اشتراکی با چند ربات)

اگر روی هاست چند ربات مختلف دارید و نمی‌خواهید پوشه‌ی کامل پروژه را آپلود کنید،
کل پروژه در **دو فایل** فشرده می‌شود:

```bash
php tools/build.php --with-secrets          # خروجی در build/
php tools/build.php --name=mybot --out=/tmp # نام و مسیر دلخواه
```

خروجی:

| فایل | نقش |
|---|---|
| `nikto-bot.php` | همه‌ی کدها + فونت فارسی + ۴۱ لوگوی ارز؛ وب‌هوک، پنل و خط فرمان |
| `nikto-cron.php` | زمان‌بند؛ کرون سرور، اجرای دائمی یا کرون اینترنتی |

فقط همین دو فایل را آپلود کنید. بار اول که اجرا شوند، فونت‌ها و لوگوها را در پوشه‌ی
`nikto-<شناسه‌ی ربات>-data/` کنار خودشان باز می‌کنند (برای هر ربات جدا، پس چند ربات
روی یک هاست با هم قاطی نمی‌شوند) و دسترسی وب به آن پوشه هم بسته می‌شود.

```bash
php nikto-bot.php doctor        # بررسی سلامت
php nikto-bot.php webhook       # ثبت وب‌هوک (آدرس از بالای همان فایل خوانده می‌شود)
php nikto-bot.php               # یا حالت پولینگ، بدون وب‌هوک
php nikto-bot.php send prices   # ارسال فوری یک کارت
php nikto-cron.php              # یک تیک زمان‌بند (برای کرون هر دقیقه)
php nikto-cron.php --loop       # اجرای دائمی زمان‌بند
```

**بدون دسترسی خط فرمان (cPanel معمولی)** — همه‌چیز از مرورگر انجام می‌شود.
مقدار `webhook_secret` بالای فایل `nikto-bot.php` نوشته شده است:

| کار | آدرس |
|---|---|
| ثبت وب‌هوک (یک بار) | `https://your-domain/nikto-bot.php?setup=<webhook_secret>` |
| صفحه‌ی عیب‌یابی | `https://your-domain/nikto-bot.php?check=<webhook_secret>` |
| کرون اینترنتی (هر دقیقه) | `https://your-domain/nikto-cron.php?key=<webhook_secret>` |

بدون کلید درست، هر سه آدرس فقط یک راهنمای کوتاه نشان می‌دهند.

توکن و شناسه‌ی مدیر در بالای `nikto-bot.php` قابل ویرایش هستند.

---

## 🛠 ابزارهای خط فرمان

```bash
php tools/doctor.php                          # بررسی سلامت نصب
php tools/selftest.php                        # تست‌های خودکار (بدون نیاز به اینترنت)
php tools/preview.php --mock                  # ساخت همه‌ی کارت‌ها با داده‌ی نمونه
php tools/preview.php --job=prices --theme=mint --out=/tmp --caption
php scheduler.php --once                      # یک بار بررسی زمان‌بندی
php tools/build.php --with-secrets            # ساخت نسخه‌ی دو فایلی
```

---

## 📁 ساختار پروژه

```
MyBOT/
├── bot.php                  اجرای ربات (long polling)
├── scheduler.php            زمان‌بند ارسال
├── webhook.php              نقطه‌ی ورود وب‌هوک
├── index.php                ریشه‌ی دامنه (وب‌هوک + صفحه‌ی وضعیت)
├── config.php               تنظیمات عمومی (مدیران، دامنه، منطقه‌ی زمانی)
├── config.local.php         توکن ربات — گیت‌نشده، با install.php ساخته می‌شود
├── assets/fonts/            فونت وزیرمتن (۶ وزن)
├── assets/coins/            ۴۱ لوگوی ارز
├── fixtures/                داده‌ی نمونه برای حالت آفلاین
├── deploy/                  systemd + نمونه‌ی nginx و htaccess
├── build/                   خروجی نسخه‌ی دو فایلی (گیت‌نشده)
├── tools/                   install · doctor · preview · selftest · build · webhook-set
└── src/
    ├── Core/                Config · Db · Settings · Http · Log · Jalali
    ├── Text/Persian.php     شکل‌دهی حروف + چینش دوسویه
    ├── Data/                PriceProvider · FearGreedProvider · CalendarProvider
    │                        EventTranslator · Coins · Countries · Mock
    ├── Render/              Canvas · Theme · Frame · Flags · CoinLogo · پنج کارت
    ├── Bundle/Runtime.php   هسته‌ی نسخه‌ی دو فایلی
    ├── Jobs/                Job · Registry · Dispatcher · Scheduler
    └── Telegram/            Api · Panel · Channels · Access · State
```

---

## ⚙️ تنظیمات کلیدی

| تنظیم | پیش‌فرض | توضیح |
|---|---|---|
| منطقه‌ی زمانی | `Asia/Tehran` | مبنای همه‌ی زمان‌بندی‌ها |
| ارقام متن | فارسی | ارقام تاریخ و متن‌های فارسی |
| ارقام داده | لاتین | ارقام قیمت و درصد |
| کیفیت تصویر | بالا | «بالا» = رندر دوبرابر و لبه‌های نرم |
| حالت ارسال | عکس | «فایل» تصویر را بدون فشرده‌سازی می‌فرستد |
| حداقل اهمیت خبر | ۲ از ۳ | فیلتر رویدادهای تقویم |
| جبران تأخیر | ۱۰ دقیقه | اگر سرور دیر بیدار شد، تا این مدت ارسال را جبران می‌کند |

---

## ❓ عیب‌یابی

| نشانه | راه‌حل |
|---|---|
| «ربات در کانال ادمین نیست» | ربات را ادمین کانال کنید و دوباره افزودن را بزنید |
| کارت ارسال نمی‌شود | `📊 وضعیت` را ببینید: کارت روشن است؟ کانال انتخاب شده؟ زمان‌بند فعال است؟ |
| ضربان زمان‌بند «متوقف؟» | `php scheduler.php` یا سرویس `nikto-scheduler` در حال اجرا نیست |
| متن فارسی مربع نشان می‌دهد | فونت‌های `assets/fonts` موجود نیستند یا GD بدون FreeType است — `php tools/doctor.php` |
| وب‌هوک کار نمی‌کند | `php tools/webhook-set.php --info` را ببینید؛ SSL دامنه و دسترسی عمومی `webhook.php` را بررسی کنید |
| لوگوی یک ارز نمایش داده نمی‌شود | لوگوی آن ارز در `assets/coins/color` نیست؛ دایره‌ای با نماد ارز جایگزین می‌شود |
| «داده‌ای دریافت نشد» | سرور به Binance/alternative.me دسترسی ندارد؛ در `config.php` مقدار `http_proxy` را تنظیم کنید |
| تصویرها خیلی سنگین‌اند | در تنظیمات، کیفیت تصویر را روی «معمولی» بگذارید |

---

## 📜 مجوز

- فونت **Vazirmatn** اثر صابر راستی‌کردار — مجوز SIL OFL 1.1 (`assets/fonts/OFL.txt`).
- لوگوی ارزها از مجموعه‌های **cryptocurrency-icons** (CC0) و **trustwallet/assets** (MIT).
