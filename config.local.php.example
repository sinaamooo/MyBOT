<?php
/**
 * 🔑 نمونه‌ی فایل کلیدها.
 *
 * این را کپی کنید به config.local.php و مقدارها را عوض کنید.
 * config.local.php داخل .gitignore است، پس هیچ‌وقت وارد گیت نمی‌شود
 * و توکن ربات از سورس بیرون می‌ماند.
 *
 * اگر این فایل را نسازید، ربات از همان مقدارهای داخل
 * bot_master_membership.php استفاده می‌کند و چیزی نمی‌شکند.
 */

define('BOT_TOKEN', 'توکن-ربات-را-اینجا-بگذارید');
define('ADMIN_ID',  123456789);              // آیدی عددی خودتان
define('CRON_KEY',  'یک-رشته-تصادفی-بلند');   // برای آدرس cron
