# 🛍️💎 سیستم فروش ممبرشیپ - راهنمای کامل

## 📋 فهرست مطالب

1. [نمای کلی](#نمای-کلی)
2. [تنظیمات اولیه](#تنظیمات-اولیه)
3. [استفاده ربات](#استفاده-ربات)
4. [پنل ادمین](#پنل-ادمین)
5. [مدیریت ممبرشیپ‌ها](#مدیریت-ممبرشیپ‌ها)
6. [مدیریت ربات‌های فرعی](#مدیریت-ربات‌های-فرعی)

## 🎯 نمای کلی

سیستم فروش ممبرشیپ شامل:

### 🤖 ربات تلگرام (Telegram Bot)
- **فایل:** `bot_master_membership.php`
- **نقش:** فروش ممبرشیپ و مدیریت اعضا
- **دکمه‌ها:** رنگی + ایموجی پریمیوم

### 🌐 پنل ادمین وب (Web Admin)
- **فایل:** `admin_panel.php`
- **نقش:** مدیریت ممبرشیپ‌ها، ربات‌ها، پرداخت‌ها
- **سبک:** رابط حرفه‌ای و زیبا

### 📊 سیستم ممبرشیپ
- ممبرشیپ‌های متعدد
- قیمت‌های مختلف (USDT/TRX/تومان)
- محدودیت تعداد اعضا
- خودکار لینک‌دهی

## ⚙️ تنظیمات اولیه

### مرحله 1: توکن‌ها

`bot_master_membership.php` را باز کنید و خطوط 11-12 را تنظیم کنید:

```php
define('BOT_TOKEN', 'توکن_ربات_مادر');  // توکن از @BotFather
define('ADMIN_ID', 8213021584);           // آیدی عددی شما
```

### مرحله 2: رمز پنل ادمین

`admin_panel.php` را باز کنید و خط 9 را تنظیم کنید:

```php
define('ADMIN_PASSWORD', 'admin123456');  // رمز جدید (خیلی محفوظ!)
```

### مرحله 3: Webhook

```bash
curl -X POST https://api.telegram.org/bot[BOT_TOKEN]/setWebhook \
  -d "url=https://yourdomain.com/bot_master_membership.php"
```

### مرحله 4: دسترسی‌های پوشه

```bash
chmod -R 755 data_master/
```

## 🤖 استفاده ربات

### برای کاربران عادی

1. **ارسال `/start`**
   ```
   👑 خوش آمدید!
   
   🛍️ سیستم فروش ممبرشیپ
   
   - پریمیوم - 50 USDT (0/100)
   - اسٹاندارد - 25 USDT (50/∞)
   ```

2. **کلیک روی 🟢 خرید**
   ```
   💳 اطلاعات پرداخت
   
   ممبرشیپ: پریمیوم
   قیمت: 50 USDT
   شناسه: pay_1692340800
   
   [🟢 تایید] [🔴 انصراف]
   ```

3. **تایید پرداخت**
   ```
   ✅ خرید موفق!
   
   ممبرشیپ: پریمیوم
   لینک: https://t.me/uploader_bot?start=mem_...
   ```

### برای مدیر (Admin)

1. **ارسال `/panel`**
   ```
   👑 پنل مدیریت
   
   [🔵 ممبرشیپ‌ها] [🔵 ربات‌ها]
   [🔵 پرداخت‌ها]  [🟢 افزودن ممبرشیپ]
   ```

## 🌐 پنل ادمین (Web Admin)

### دسترسی

```
URL: https://yourdomain.com/admin_panel.php
رمز: admin123456
```

### صفحه اول (Dashboard)

نمایش‌دهی:
- 👥 کاربران: تعداد کل کاربران
- 🛍️ ممبرشیپ‌ها: تعداد کل ممبرشیپ‌ها
- 🤖 ربات‌ها: تعداد ربات‌های وصل‌شده
- 💰 پرداخت‌های تایید شده: تعداد پرداخت‌های موفق

### بخش‌های پنل

#### 1️⃣ افزودن ممبرشیپ

```
نام: پریمیوم
قیمت: 50
واحد: USDT
محدودیت: 100
```

**نتیجه:** ممبرشیپ جدید ایجاد می‌شود و کاربران می‌توانند خریداری کنند.

#### 2️⃣ افزودن ربات

```
توکن: 123456:ABC...
نام کاربری: uploader_bot
ممبرشیپ: پریمیوم
```

**نتیجه:** ربات به این ممبرشیپ متصل می‌شود و اعضا به آن لینک شده می‌شوند.

#### 3️⃣ مشاهده پرداخت‌ها

جدول تمام پرداخت‌ها:
- کاربر (نام کاربری)
- ممبرشیپ (نام ممبرشیپ خریداری‌شده)
- مبلغ و واحد
- وضعیت (✅ تایید یا ⏳ در انتظار)
- تاریخ و ساعت

## 🛍️ مدیریت ممبرشیپ‌ها

### ایجاد ممبرشیپ جدید

**از طریق پنل ادمین:**

```
🛍️ افزودن ممبرشیپ

نام ممبرشیپ:    پریمیوم
قیمت:           50
واحد پول:       USDT
محدودیت اعضا:  100
```

### ویژگی‌های ممبرشیپ

| ویژگی | توضیح |
|------|--------|
| نام | نام نمایش‌شده برای کاربران |
| قیمت | مبلغ پرداخت (عدد) |
| واحد | USDT / TRX / تومان |
| محدودیت | تعداد اعضایی که می‌توانند خریداری کنند (0 = نامحدود) |

### مثال‌های عملی

**ممبرشیپ 1:**
```
نام: اسٹاندارد
قیمت: 25
واحد: USDT
محدودیت: ∞ (نامحدود)
```

**ممبرشیپ 2:**
```
نام: پریمیوم VIP
قیمت: 100
واحد: USDT
محدودیت: 50 (فقط 50 نفر)
```

**ممبرشیپ 3:**
```
نام: تومانی
قیمت: 500000
واحد: تومان
محدودیت: 200
```

## 🤖 مدیریت ربات‌های فرعی

### اتصال ربات اپلودری

**از طریق پنل ادمین:**

```
🤖 افزودن ربات

توکن ربات:   123456:ABC-DEF...
نام کاربری:  @uploader_bot
ممبرشیپ:    پریمیوم
```

### ربات فرعی چه‌طور کار می‌کند؟

1. **مرحله 1: ایجاد ربات**
   - مدیر توکن ربات اپلودری را وارد می‌کند
   - ربات به ممبرشیپ متصل می‌شود

2. **مرحله 2: خریداری ممبرشیپ**
   - کاربر ممبرشیپ را خریداری می‌کند
   - لینک خودکار ایجاد می‌شود

3. **مرحله 3: دسترسی**
   - کاربر لینک را باز می‌کند
   - ربات فرعی فایل‌ها را می‌فرستد

### مثال کامل

```
1. مدیر یک ممبرشیپ می‌سازد: "فیلم‌های درسی - 50 USDT"
2. مدیر یک ربات اپلودری وصل می‌کند
3. کاربر روی 🟢 خرید می‌زند
4. کاربر پرداخت را تایید می‌کند
5. خودکار لینک دریافت می‌کند: https://t.me/filmbot?start=mem_...
6. کلیک می‌کند و فیلم‌ها دریافت می‌کند
```

## 💳 سیستم پرداخت

### جریان پرداخت

```
کاربر → خرید کلیک → اطلاعات پرداخت → تایید → لینک
```

### وضعیت‌های پرداخت

- **⏳ در انتظار (Pending):** اول که پرداخت ثبت شود
- **✅ تایید شده (Confirmed):** بعد از تایید پرداخت

### مثال پرداخت

```
ممبرشیپ: پریمیوم
قیمت: 50 USDT
شناسه: pay_1692340800

مراحل:
1. کاربر 50 USDT به کیف‌پول فرستد
2. مدیر شناسه را بررسی کند
3. مدیر 🟢 تایید کند
4. کاربر لینک دریافت کند
```

## 📊 دکمه‌های رنگی + ایموجی

### دکمه‌های ربات

- 🟢 **سبز:** خرید / تایید / اقدام مثبت
- 🔴 **قرمز:** انصراف / حذف / اقدام منفی
- 🔵 **آبی:** منو / تنظیمات / اقدام عمومی

### مثال‌ها

```
🛍️ ممبرشیپ خرید
[🟢 خرید] [🔴 بعداً] [🔵 اطلاعات]

💳 پرداخت تایید
[🟢 تایید کردم] [🔴 انصراف]

👑 پنل مدیریت
[🔵 ممبرشیپ‌ها] [🔵 ربات‌ها]
[🔵 پرداخت‌ها] [🟢 افزودن]
```

## 📂 ساختار داده

```
data_master/
├── memberships.json  # ممبرشیپ‌ها
│   └── m_1692340800: {
│       id, name, price, currency,
│       members[], limit, bot_id
│   }
├── bots.json         # ربات‌های فرعی
│   └── bot_1692340800: {
│       id, token, username, membership_id
│   }
├── payments.json     # پرداخت‌ها
│   └── pay_1692340800: {
│       id, user_id, membership_id,
│       amount, currency, status
│   }
└── users.json        # کاربران
    └── 8213021584: {
        telegram_id, username, membership_id
    }
```

## 🔐 نکات امنیتی

✅ **رمز پنل:**
```php
define('ADMIN_PASSWORD', 'رمز_خیلی_محفوظ');
```

✅ **آیدی ادمین:**
- فقط `ADMIN_ID` می‌تواند `/panel` را استفاده کند
- فقط `ADMIN_ID` می‌تواند ممبرشیپ اضافه کند

✅ **دسترسی پوشه:**
```bash
chmod 755 data_master/
```

✅ **توکن‌های ربات:**
- در `data_master/bots.json` ذخیره می‌شوند
- از دسترسی عمومی محافظت کنید

## 🚀 شروع سریع (3 دقیقه)

### قدم 1: تنظیم
```
bot_master_membership.php → BOT_TOKEN + ADMIN_ID
admin_panel.php → ADMIN_PASSWORD
```

### قدم 2: Webhook
```bash
curl -X POST https://api.telegram.org/bot[TOKEN]/setWebhook \
  -d "url=https://yourdomain.com/bot_master_membership.php"
```

### قدم 3: پنل
```
https://yourdomain.com/admin_panel.php
رمز: admin123456
```

### قدم 4: ممبرشیپ اضافه کنید
```
نام: پریمیوم
قیمت: 50 USDT
محدودیت: 100
```

### قدم 5: ربات اضافه کنید
```
توکن: از @BotFather
نام: @uploader_bot
```

### قدم 6: تست
```
ربات: /start
ادمین: /panel
```

## 🎉 آماده‌اید!

حالا می‌توانید:
- ✅ ممبرشیپ بفروشید
- ✅ اعضا را مدیریت کنید
- ✅ ربات‌های فرعی وصل کنید
- ✅ پرداخت‌ها را ردیابی کنید
- ✅ داشبورد را مشاهده کنید

---

**نیاز به کمک؟** پنل ادمین را بررسی کنید یا ربات را `/panel` ارسال کنید.
