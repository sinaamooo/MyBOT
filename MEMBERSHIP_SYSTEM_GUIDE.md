# 🛍️ فروشگاه تلگرام + ربات‌های اپلودر — راهنمای کامل

دو فایل PHP، بدون دیتابیس، بدون کتابخانه بیرونی.

| فایل | نقش |
|------|------|
| `bot_master_membership.php` | ربات مادر (فروشگاه) + ربات‌های اپلودر + منطق داده |
| `admin_panel.php` | پنل مدیریت وب |

---

## ⚙️ نصب

### ۱. تنظیم فایل ربات
خطوط ۲۴–۲۷ فایل `bot_master_membership.php`:

```php
if (!defined('BOT_TOKEN')) define('BOT_TOKEN', 'توکن_ربات_مادر');
if (!defined('ADMIN_ID'))  define('ADMIN_ID',  8213021584);
if (!defined('CRON_KEY'))  define('CRON_KEY',  'یک-کلید-تصادفی');
```

### ۲. رمز پنل
خط ۱۰ فایل `admin_panel.php`:
```php
define('ADMIN_PASSWORD', 'رمز_قوی');
```

### ۳. وبهوک
پنل → **📊 داشبورد** → «تنظیم وبهوک ربات مادر»

### ۴. کران (برای دقت حذف خودکار)
در کران‌جاب هاست، هر دقیقه:
```
https://دامنه/bot_master_membership.php?cron=کلید-کران
```

### ۵. محافظت از داده
```apache
# در data_master/.htaccess
Deny from all
```

---

## 📱 منوی ربات مادر

دکمه‌ها به شکل **منو** (کیبورد پایین صفحه) نمایش داده می‌شوند:

```
┌─────────────────────────────┐
│      🛒 خرید محصول          │
├──────────────┬──────────────┤
│ 👤 حساب کاربری│➕ افزایش موجودی│
├──────────────┴──────────────┤
│      👥 زیر مجموعه گیری      │
├──────────────┬──────────────┤
│📊 پیگیری سفارش│  📞 پشتیبانی  │
├──────────────┴──────────────┤
│ 💚 چطوری میتوانم اعتماد کنم  │
└─────────────────────────────┘
```

### ✏️ ویرایش دکمه‌ها و متن‌ها — داخل خود ربات

> همه ویرایش‌ها از **داخل ربات** انجام می‌شود، نه پنل وب.
> دلیلش این است که در تلگرام می‌توانید **ایموجی پریمیوم** را مستقیم تایپ کنید.

در ربات `/panel` را بزنید:

```
👑 پنل مدیریت
├── 🎨 دکمه‌ها
├── 📝 متن‌ها
├── 💠 رنگ دکمه‌های شیشه‌ای
├── 🧾 سفارش‌ها      🤖 ربات‌ها
├── 📢 کانال‌ها       📢 پیام همگانی
└── 🌐 پنل وب
```

### 🎨 دکمه‌ها

روی هر دکمه بزنید تا ویرایشش کنید:

| گزینه | کار |
|-------|-----|
| ✏️ متن | متن جدید را بفرستید — **ایموجی پریمیوم** هم می‌گیرد |
| 😀 ایموجی | ایموجی معمولی ابتدای دکمه |
| 🎨 رنگ | چرخش بین 🔵 آبی → 🟢 سبز → 🔴 قرمز → بدون رنگ |
| ✨ پریمیوم | ایموجی پریمیوم بفرستید یا کدش را |
| 📐 ردیف · 🔢 ترتیب | جای دکمه |
| ✅/❌ | روشن یا خاموش |

پایین صفحه: **🔄 حالت منو/شیشه‌ای** · **📐 چیدمان** · **📌 چسبان** · **➕ دکمه جدید**

### 📝 متن‌ها

۱۷ متن ربات، هرکدام با:

- **✏️ تغییر متن** — متن جدید را با ایموجی پریمیوم، نقل‌قول، پررنگ و لینک بفرستید؛ همه حفظ می‌شود
- **❝ نقل‌قول** و **❝ نقل‌قول بازشو** — با یک دکمه کل متن را داخل نقل‌قول می‌برد یا برمی‌دارد
- **♻️ بازگردانی پیش‌فرض**

### 🔤 متن دکمه‌های ثابت

📝 متن‌ها → **🔤 متن دکمه‌های ثابت** — متن ۱۸ دکمه‌ای که همه‌جای ربات تکرار می‌شوند:

بازگشت · منوی اصلی · انصراف · تایید · رد · پنل · خرید · دریافت مجدد ·
تکمیل ظرفیت · ارسال رسید · پرداخت از کیف پول · پرداخت مستقیم ·
افزایش موجودی · سفارش‌های من · ورود به ربات · دریافت لینک · باز کردن · بستن منو ·
ارتباط مستقیم · ارتباط غیر مستقیم

هنگام فرستادن متن، **ایموجی پریمیوم** هم بگذارید — روی همان دکمه می‌نشیند.

### 💠 رنگ دکمه‌های شیشه‌ای

۱۲ نقش — روی هرکدام بزنید تا رنگش عوض شود:
خرید · تایید · انصراف · رد · بازگشت · اطلاعات · پنل · لینک · پشتیبانی · کانال · عضو شدم · آپلود

### ✨ ایموجی پریمیوم

ساده‌ترین راه: **همان‌جا در ربات تایپش کنید.** موقع فرستادن متن دکمه یا متن ربات،
ایموجی پریمیوم بگذارید — ربات خودش نگهش می‌دارد.

برای گرفتن کد خام: `/emoji` بزنید و پیامی با ایموجی پریمیوم بفرستید.

> ایموجی پریمیوم روی دکمه نیاز دارد صاحب ربات پریمیوم داشته باشد
> یا ربات از Fragment یوزرنیم خریده باشد — محدودیت خود تلگرام است.

### 📐 چیدمان دلخواه — بدون محدودیت

**ستون ردیف** تعیین می‌کند دکمه کجا بنشیند. هر دکمه‌ای که ردیف یکسان دارد کنار هم می‌آید:

```
ردیف ۱ → خرید محصول ، حساب کاربری      (دو تا بالا)
ردیف ۲ → افزایش موجودی                  (یکی پایین)
ردیف ۳ → پشتیبانی ، قوانین ، درباره ما  (سه تا)
```

یا در 🎨 دکمه‌ها → **📐 چیدمان** بنویسید `3,2,1` — یعنی ۳ بالا، ۲ وسط، ۱ آخر.
الگو تکرار نمی‌شود؛ دکمه‌های اضافه تک‌تک می‌آیند.

### 🎨 رنگ واقعی دکمه‌ها

از **Bot API 9.4** (۹ فوریه ۲۰۲۶) تلگرام فیلد `style` را به `KeyboardButton` و
`InlineKeyboardButton` اضافه کرد:

```json
{"text": "خرید محصول", "style": "success"}
```

سه رنگ: `primary` (آبی) · `success` (سبز) · `danger` (قرمز) — روی **هر دو نوع دکمه**.
اگر سرور Bot API قدیمی‌تر باشد، ربات خودکار بدون رنگ دوباره می‌فرستد و چیزی خراب نمی‌شود.

### 🔓 بستن منو

منوی کیبورد دیگر **چسبان نیست** — کاربر می‌تواند با دکمه کیبورد تلگرام ببنددش،
یا `/hide` را بزند. با `/menu` برمی‌گردد.

اگر می‌خواهید منو همیشه باز بماند (کاربر نتواند ببندد)، در 🎨 دکمه‌ها
گزینه **📌 چسبان** را روشن کنید — ولی توصیه نمی‌شود.

---

## 📢 کانال اعلام فروش

پنل → **👥 رفرال** → بخش «کانال اعلام فروش»

هر خرید موفق خودکار در یک کانال جدا اعلام می‌شود:

```
🎉 فروش جدید

📦 محصول: اشتراک طلایی
🧾 کد خرید: or_tjs2lzddec64
💰 مبلغ: 250,000 تومان
👥 تعداد ممبر: 7 از 100
🎯 باقی‌مانده: 93
📅 2026-08-14 21:05
```

**متغیرها:** `{product}` `{code}` `{amount}` `{currency}` `{count}` `{limit}`
`{remaining}` `{limit_part}` `{user}` `{user_id}` `{date}`

قالب کاملا قابل ویرایش است و دکمه **ارسال پیام آزمایشی** دارد.
ربات مادر باید در آن کانال ادمین باشد.

> نمایش نام خریدار پیش‌فرض **خاموش** است تا اطلاعات مشتری‌ها عمومی نشود.
> اگر می‌خواهید نشان داده شود تیکش را بزنید.

---

## 👥 تب رفرال

پنل → **👥 رفرال**: تعداد معرف فعال، کاربران معرفی‌شده، مجموع پورسانت پرداختی،
درصد فعلی، و **جدول برترین معرف‌ها** با تعداد زیرمجموعه و درآمد هرکدام.
درصد پورسانت از همین‌جا قابل تغییر است.

---

## 🔧 راه‌اندازی خودکار

> ### 🔘 مهم: خودِ دکمه شیشه‌ای یک محصول است
>
> سه دکمه‌ای که زیر «🛒 خرید محصول» گذاشته‌اید (مثلا **غیراخلاقی**، **ممبر اخلاقی**،
> **ممبر ویژه**) خودشان محصول‌اند. **لازم نیست هیچ محصولی جدا بسازید.**
> مشتری روی هرکدام بزند، مستقیم می‌رود سراغ لینک کانال.
>
> قیمت، حداقل/حداکثر تعداد، و سرعت‌ها روی **خود دکمه** ذخیره می‌شوند.

در ربات دستور **`/setup`** را بزنید (یا `/panel` → 🔧 راه‌اندازی خودکار).

خودش این کارها را می‌کند:
- جریان سفارش (لینک ← تعداد ← سرعت ← ادمین کردن ← فاکتور) را روی **هر دکمه** روشن می‌کند
- سه سرعت پیش‌فرض و حداقل/حداکثر تعداد را می‌گذارد
- دکمه «خرید محصول» را روشن می‌کند
- گزارش می‌دهد کدام دکمه هنوز قیمت ندارد

> **هیچ دکمه‌ای حذف یا اضافه نمی‌شود و هیچ محصولی ساخته نمی‌شود.**

```
🔧 راه‌اندازی خودکار

✅ 3 دکمه آمادهٔ فروش شد (خود دکمه = محصول)

🛒 دکمه‌های فروش: 3
   ✅ 🏝 غیراخلاقی — قیمت ندارد
   ✅ 💼 ممبر اخلاقی — قیمت ندارد
   ✅ 👤 ممبر ویژه — قیمت ندارد

⚠️ باقی‌مانده:
• قیمت این دکمه‌ها صفر است: غیراخلاقی، ممبر اخلاقی، ممبر ویژه
   /panel → 🔘 دکمه‌ها → روی دکمه → 💰 قیمت
```

بعدش **فقط قیمت هرکدام را بگذارید** و تمام:

```
/panel  →  🔘 دکمه‌ها  →  🛒 خرید محصول  →  روی «غیراخلاقی»
        →  💰 قیمت  →  عدد را بفرستید (مثلا 5000)
```

روی همان صفحه این‌ها هم هست:

| دکمه | کار |
|------|-----|
| 💰 قیمت | قیمت هر ۱۰۰۰ عدد |
| 👥 تعداد | حداقل و حداکثر — مثلا `1000-100000` |
| ⚡️ سرعت‌ها | متن، ایموجی، رنگ، **ضریب قیمت** و روشن/خاموش هر سرعت |
| ✏️ متن · 😀 ایموجی · 🎨 رنگ · ✨ پریمیوم | ظاهر خود دکمه |

### 💰 قیمت‌گذاری از پنل وب

پنل → **🛒 محصولات** → برای هر دکمه یک کارت کامل:

| فیلد | کار |
|------|-----|
| قیمت پایه | مثلا `7500` |
| به ازای هر چند نفر؟ | معمولا `1000` |
| واحد پول | تومان / USDT / TRX |
| حداقل / حداکثر تعداد سفارش | مثلا `2000` تا `60000` |
| توضیح کوتاه | زیر نام محصول |
| ⚡️ جدول سرعت‌ها | **متن** · **ایموجی** · **ضریب قیمت** · **نفر در روز** · **رنگ** · **توضیح** · روشن/خاموش |

جدول سرعت‌ها همه چیز را همان‌جا می‌دهد — متن دکمه، ایموجی، رنگ، عددها و توضیح.
قیمت نهایی هرکدام زنده حساب می‌شود و پایین کارت یک **نمونه فاکتور** می‌بینید:

```
ایموجی  متن دکمه     ضریب  نفر/روز  قیمت هر 1,000  رنگ      روشن
 🐌     خیلی کند      1      300      6,000 تومان   🔵 آبی    ✅
 🚶     متوسط        1.3    2,000     7,800 تومان   🟢 سبز    ☐
 🚀     برق‌آسا        2     15,000   12,000 تومان   🔴 قرمز   ✅
        توضیح: برای کانال‌های بزرگ

🧾 نمونه فاکتور — 5,000 نفر با «🐌 خیلی کند»: 30,000 تومان · ⏳ حدود 17 روز
```

متن روی دکمه از سه تای اول ساخته می‌شود: `🚀 برق‌آسا — 15,000/روز`
سرعتی که تیکش را بردارید، به مشتری نشان داده نمی‌شود.

> **چه چیزی فقط داخل ربات است؟** تنها ✨ **ایموجی پریمیوم** — چون فقط در تلگرام تایپ می‌شود.
> بقیه (متن، ایموجی، رنگ، عددها) از هر دو جا قابل ویرایش است.

### محصول جداگانه — کِی لازم است؟

فقط وقتی محصولی می‌فروشید که **فایل تحویل می‌دهد** (لینک ربات اپلودر) یا محدودیت
تعداد خریدار دارد. آن‌وقت از پنل وب محصول بسازید و از نوع دکمه، «رفتن به محصول» را
انتخاب و وصلش کنید. برای فروش ممبر لازم نیست.

---

## 🛒 جریان سفارش محصول

اگر برای محصولی **جریان سفارش** را روشن کنید، ربات مرحله‌به‌مرحله از مشتری می‌پرسد:

```
کاربر روی دکمهٔ شیشه‌ای می‌زند (مثلا «غیراخلاقی») — خودِ دکمه محصول است
        ↓
🔗 لینک کانال/گروه خود را بفرستید
        ↓
👥 مقدار سفارش؟  (حداقل 1000 · حداکثر 100000)
        ↓
💰 نرخ هر کا ممبر: 250,000 تومان
        ↓
⚡️ سرعت تکمیل؟   🐢 کند · 🚶 متوسط · ⚡️ سرعت بالا
        ↓
🤖 ربات را در کانال ادمین کنید
   [➕ افزودن ربات به کانال]
   [✅ ادمین کردم]          ← تا ادمین نشود جلو نمی‌رود
        ↓
📋 فاکتور کامل
        ↓
✅ موافقم، نهایی سازی → پرداخت
```

### 🤖 مرحله «ادمین کردن ربات»

بعد از انتخاب سرعت، متن افزودن ربات با دو دکمه می‌آید. تایید به **دو روش** انجام می‌شود:

- **کانال عمومی** (`t.me/name`) — ربات مستقیم با API چک می‌کند ادمین هست یا نه
- **کانال خصوصی** (`t.me/+hash`) — به‌محض اینکه کاربر ربات را ادمین کند،
  تلگرام رویداد می‌فرستد و ربات همان لحظه تایید می‌کند و **خودکار فاکتور را نشان می‌دهد**

اگر ادمین نشده باشد و دکمه «✅ ادمین کردم» را بزند، رد می‌شود و دوباره راهنمایی می‌بیند.

شناسه و عنوان کانال روی سفارش ذخیره می‌شود و در اعلان ادمین می‌آید:
`🤖 ربات ادمین است — کانال من`

**محاسبه:** `قیمت × ضریب سرعت × (تعداد ÷ per)`
مثلا 250,000 تومان به ازای هر 1000 نفر، برای 5000 نفر با سرعت عادی = 1,250,000 تومان.

**متن‌های قابل ویرایش** (در ربات → 📝 متن‌ها):
سوال لینک · لینک نامعتبر · سوال تعداد · تعداد نامعتبر · سوال سرعت · نمایش نرخ · فاکتور · قالب هر سفارش

### ⚡️ سرعت‌ها

سه سرعت پیش‌فرض، هرکدام با **ضریب قیمت** و **رنگ** جدا:

| سرعت | ضریب | رنگ |
|------|------|-----|
| 🐢 کند | ×1 | 🔵 آبی |
| 🚶 متوسط | ×1.3 | 🟢 سبز |
| ⚡️ سرعت بالا | ×1.7 | 🔴 قرمز |

`/panel` → 🔘 دکمه‌ها → روی دکمه → ⚡️ سرعت‌ها → روی هر سرعت:

| دکمه | کار | نمونه |
|------|-----|-------|
| ✏️ متن دکمه | نام سرعت | `نیمه‌سریع` |
| 😀 ایموجی | ایموجی جلوی نام | `🏃` |
| ✨ پریمیوم | ایموجی پریمیوم (فقط در تلگرام تایپ می‌شود) | — |
| 🎨 رنگ | 🔵 آبی / 🟢 سبز / 🔴 قرمز | — |
| ✖️ ضریب قیمت | قیمت این سرعت = قیمت پایه × ضریب | `1.3` |
| 🚀 نفر در روز | چند نفر در روز تحویل می‌شود | `3500` |
| 💬 توضیح | یک خط توضیح، زیر متن انتخاب سرعت | `مناسب اکثر کانال‌ها` |

روی دکمه فقط **ایموجی و نام** می‌آید — قیمت و عدد روز رویش نوشته نمی‌شود:

```
👉 لطفا سرعت تکمیل سفارش را انتخاب کنید؟

🏃 نیمه‌سریع — مناسب اکثر کانال‌ها (حدود 2 روز)

[🐢 کند]  [🏃 نیمه‌سریع]  [⚡️ سرعت بالا]
```

عدد «نفر در روز» پشت صحنه کار می‌کند: زمان تحویل را می‌سازد
(۷٬۰۰۰ نفر ÷ ۳٬۵۰۰ = **حدود ۲ روز**) و در متن نرخ و فاکتور نشان داده می‌شود.

> اگر خواستید عدد روی دکمه هم بیاید:
> پنل → ⚙️ تنظیمات → «نفر در روز روی دکمه سرعت هم نوشته شود».

(برای محصول جداگانه: `/panel` → 🛒 محصولات → محصول → ⚡️ سرعت‌ها)

### 🛒 مدیریت دکمهٔ فروش در ربات

`/panel` → **🔘 دکمه‌ها** → روی دکمه:
✏️ متن · 💰 قیمت · 👥 تعداد (حداقل-حداکثر) · ⚡️ سرعت‌ها ·
😀 ایموجی · 🎨 رنگ · ✨ ایموجی پریمیوم · 📐 ردیف · 🔢 ترتیب · روشن/خاموش

**متغیرها:** `{link}` `{qty}` `{product}` `{speed}` `{rate}` `{total}` `{currency}` `{per}` `{min}` `{max}`

---

## 💠 قیمت‌گذاری روی هر ممبر

پنل → 🛒 محصولات → بالای هر کارت قیمت هر یک ممبر را زنده نشان می‌دهد:

```
💠 قیمت هر ۱ ممبر: 7.5 تومان — یعنی 7,500 برای هر 1,000 نفر.
```

دو راه، هر دو درست:

| می‌خواهید | «قیمت پایه» | «به ازای هر چند نفر؟» |
|-----------|-------------|------------------------|
| قیمت هر **۱ ممبر** | `25` | `1` |
| قیمت هر **۱۰۰۰ ممبر** | `25000` | `1000` |

نتیجه یکی است — هرکدام برایتان راحت‌تر است.

---

## 🔄 دکمه تازه که از همان اول کار می‌کند

`/panel` → 🔘 دکمه‌ها → 🛒 خرید محصول → **➕ افزودن دکمه شیشه‌ای**

دکمه تازه همه تنظیمات را از یک دکمه آماده **به ارث می‌برد** — قیمت، حدود تعداد،
سرعت‌ها، گروه گزارش و ربات تحویل — پس همان لحظه قابل فروش است:

```
✅ دکمه ممبر ویژه ساخته شد و آماده فروش است.

🧬 تنظیمات از «غیراخلاقی» کپی شد:
💰 قیمت: 5,000 تومان
👥 تعداد: 2,000 تا 60,000
⚡️ سرعت‌ها: 3
📢 گروه گزارش: -1001111 — تاپیکش را بگذارید

[💰 قیمت] [😀 ایموجی]  [📢 گزارش خرید]  [⚙️ همه تنظیمات]
```

**شماره تاپیک** کپی نمی‌شود چون هر محصول باید تاپیک خودش را داشته باشد.

### 🔄 هماهنگ کردن دکمه‌های قدیمی

اگر دکمه‌هایی دارید که تنظیم نشده‌اند:

`/panel` → 🔘 دکمه‌ها → 🛒 خرید محصول → **🔄 هماهنگ کردن همه با هم**
→ روی دکمه‌ای بزنید که درست تنظیم شده.

```
کپی می‌شود:      💰 قیمت · 👥 تعداد · ⚡️ سرعت‌ها · 📢 گروه گزارش · 🤖 ربات تحویل
دست‌نخورده:      نام · ایموجی · 🧵 شماره تاپیک هر محصول
```

---

## 🤖 ربات تحویل هر محصول

هر محصول می‌تواند **ربات اپلودر جدا** داشته باشد — برای وقتی که محصول فایل تحویل می‌دهد.

`/panel` → 🔘 دکمه‌ها → روی محصول → **🤖 ربات تحویل**

فهرست ربات‌های اپلودرتان می‌آید؛ یکی را انتخاب کنید و **🔗 کد لینک** محتوا را بگذارید.
بعد از خرید، لینک از همان ربات به مشتری داده می‌شود.
برای فروش ممبر لازم نیست.

---

## 🧪 حالت تست — سفارش با ۰ ریال

پنل → ⚙️ تنظیمات → **🧪 حالت تست**

با روشن بودنش، سفارش با مبلغ صفر **تا آخر می‌رود**: بدون پرداخت خودکار تایید می‌شود،
کمپین قفل کانالش ساخته می‌شود و در پیگیری سفارش «در حال انجام» می‌شود.

```
🧪 سفارش تستی ثبت شد

🧾 کد پیگیری: or_tjwujb8031be
📦 غیراخلاقی
👥 5,000 نفر · ⚡️ سرعت بالا
💳 مبلغ: 0 تومان
📊 وضعیت: در حال انجام
```

برای امتحان کردن کل مسیر قبل از قیمت‌گذاری. **یادتان باشد بعد خاموشش کنید.**

---

## 📢 گزارش خرید در گروه (با تاپیک)

یک گروه بسازید و برای هر محصول یک **تاپیک** جدا — گزارش هر محصول در بخش خودش می‌افتد.

```
گروه «گزارش فروش»
├─ 🧵 تاپیک 11  →  🏝 غیراخلاقی
├─ 🧵 تاپیک 22  →  💼 ممبر اخلاقی
└─ 🧵 تاپیک 33  →  👤 ممبر ویژه
```

### تنظیم از پنل

پنل → 🛒 محصولات → روی هر کارت → **📢 گزارش خرید در گروه**

| فیلد | مقدار |
|------|-------|
| روشن باشد | ✔ |
| آیدی گروه | `-1001234567890` |
| شماره تاپیک | `11` (۰ = بدون تاپیک) |
| متن گزارش | با متغیرها و HTML |
| دو دکمه | متن + لینک + رنگ + روشن/خاموش |

**متغیرها:**
`{product}` `{emoji}` `{qty}` `{speed}` `{per_day}` `{eta}` `{amount}` `{currency}`
`{code}` `{link}` `{channel}` `{user}` `{user_id}` `{date}` `{delivered}`

دکمه **🧪 ارسال گزارش آزمایشی** همان‌جا هست تا قبل از فروش واقعی امتحان کنید.

### تنظیم از داخل ربات

`/panel` → **📢 گزارش خرید در گروه** — همه محصولات یک‌جا:

```
📢 گزارش خرید در گروه

✅ 🏝 غیراخلاقی — 🧵 11
✅ 💼 ممبر اخلاقی — 🧵 22
❌ 👤 ممبر ویژه — تنظیم نشده

[✅ 🏝 غیراخلاقی]  [✅ 💼 ممبر اخلاقی]  [❌ 👤 ممبر ویژه]
[👥 یک گروه برای همه]
```

**👥 یک گروه برای همه** آیدی گروه را یک‌بار روی همه محصولات می‌نشاند و همه را روشن می‌کند؛
بعد فقط شماره تاپیک هرکدام را جدا بگذارید.

روی هر محصول: 👥 آیدی گروه · 🧵 شماره تاپیک · ✏️ متن گزارش · 1️⃣2️⃣ دو دکمه · 🧪 ارسال آزمایشی

### متن با ایموجی پریمیوم و نقل‌قول

ایموجی پریمیوم را فقط در تلگرام می‌شود تایپ کرد، پس متن گزارش را از داخل ربات بنویسید:

```
/panel → 📢 گزارش خرید در گروه → روی محصول → ✏️ متن گزارش
```

هرچه بفرستید عیناً حفظ می‌شود — ✨ ایموجی پریمیوم، **پررنگ**، نقل‌قول و نقل‌قول بازشونده.
همان صفحه آیدی گروه، شماره تاپیک و هر دو دکمه را هم دارد.

نتیجه در گروه:

```
🎉 فروش جدید                     ← تاپیک 11

▌📦 غیراخلاقی
▌👥 5,000 ممبر · ⚡️ سرعت بالا

💰 42,500 تومان
🧾 or_tjwuje5df446

[🛒 ثبت سفارش]
[📞 پشتیبانی]
```

دکمه بدون لینک نشان داده نمی‌شود. اگر ارسال شکست بخورد (ربات در گروه ادمین نیست،
تاپیک اشتباه است)، خطای دقیقش برای شما پیام می‌شود.

---

## 💳 پرداخت — بدون دکمه، خودکار

هیچ دکمه «پرداخت مستقیم» یا «پرداخت از کیف پول» وجود ندارد. ربات خودش تصمیم می‌گیرد:

```
✅ موافقم، نهایی سازی سفارش
        ↓
   موجودی کافی است؟
        ├── بله → از کیف پول کم می‌شود، سفارش همان‌جا ثبت و شروع می‌شود
        └── نه  → می‌رود صفحه شارژ، با یک دکمه به اندازه دقیق کمبود
```

### موجودی کافی

بدون هیچ سوالی:

```
✅ سفارش شما تایید شد!

📦 محصول: ممبر ویژه
📣 کانال: https://t.me/mychannel
👥 تعداد: 5,000 نفر
⚡️ سرعت: ⚡️ سرعت بالا
💰 مبلغ: 2,975,000 تومان
🧾 کد پیگیری: or_tjx8kv4f7302
📊 وضعیت: 🔄 در حال انجام
```

### موجودی ناکافی

```
❌ موجودی ناکافی!

💰 موجودی شما: 0 تومان
💳 مبلغ سفارش: 2,975,000 تومان
➖ کمبود: 2,975,000 تومان

👇 دکمه زیر را بزنید، یا مبلغ دلخواهتان را بفرستید.
بعد از تایید شارژ، سفارش شما خودکار ثبت می‌شود.

[💳 شارژ 2,975,000 تومان]      ← یک دکمه، دقیقا به اندازه کمبود
```

**تا اینجا هیچ سفارشی ثبت نشده.** فقط مشخصاتش نگه داشته می‌شود.

### شارژ

```
💳 درخواست شارژ ثبت شد

💰 مبلغ: 2,975,000 تومان
🏦 روش: کارت به کارت

💠 مقصد پرداخت:
6037-1111-2222-3333
به نام: علی محمدی

🧾 کد پیگیری: or_tjx8kv733bad

👇 مبلغ را واریز کنید، بعد دکمه «ارسال رسید» را بزنید.

🛒 بعد از تایید شارژ، سفارش «ممبر ویژه» خودکار ثبت می‌شود.

[🧾 ارسال رسید]
```

شما که رسید را تایید کنید، **دو کار هم‌زمان انجام می‌شود**: کیف پول شارژ می‌شود و
سفارش نیمه‌تمام خودش ثبت و شروع می‌شود. مشتری هیچ دکمه‌ای نمی‌زند.

اگر مبلغ شارژ کمتر از سفارش باشد، پول در کیف پول می‌ماند و فقط کمبود باقی‌مانده خواسته می‌شود.

> ⚠️ **شماره کارت را حتما در پنل وب ← ⚙️ تنظیمات بگذارید.** بدون آن هیچ شارژی
> ممکن نیست؛ به مشتری «با پشتیبانی تماس بگیرید» گفته می‌شود و به شما هشدار می‌رسد.

---

## 📊 پیگیری سفارش — فقط با کد

دکمه **📊 پیگیری سفارش** فهرست بلند نشان نمی‌دهد. فقط کد می‌خواهد:

```
🔍 پیگیری سفارش

کد پیگیری سفارش خود را بفرستید.
نمونه: or_tjwodm15a1a3

[🔄 در حال انجام · غیراخلاقی]   ← ۵ سفارش آخر، فقط دکمه
[✅ انجام شد · ممبر ویژه]
[🔴 انصراف]
```

سه راه، هر سه یک نتیجه: کد را همین‌طوری بفرستد · `/track کد` · یکی از دکمه‌های بالا.

```
🔍 وضعیت سفارش

🧾 کد پیگیری: or_tjwsngc211e1
📊 وضعیت: 🔄 در حال انجام

🛒 غیراخلاقی
📣 https://t.me/chanB
👥 10,000 نفر · 🐢 کند
📊 ███░░░░░░░ 31%  (3,100/10,000)
⏳ باقی‌مانده: 6,900 نفر · حدود 14 روز

💰 مبلغ: 85,000 تومان
```

### وضعیت‌ها

| وضعیت | یعنی |
|-------|------|
| ⏳ منتظر پرداخت | هنوز رسید نفرستاده |
| 🧾 رسید در حال بررسی | منتظر تایید شما |
| 🔄 در حال انجام | ممبرها در حال اضافه شدن |
| ⏸ متوقف | ربات در کانال ادمین نیست |
| ✅ انجام شد | تعداد سفارش کامل شد |
| ❌ رد شده | رسید رد شد |

کد پیگیری بلافاصله بعد از تایید فاکتور به مشتری داده می‌شود.

---

## 📋 لیست تعرفه‌ها

یک دکمه شیشه‌ای زیر بخش خرید محصول. جدول قیمت‌ها را **خودکار** می‌سازد:

```
📋 تعرفه‌های ما

🏝 غیراخلاقی
   🐢 کند — 5,000 تومان / 1,000 نفر · 🚀 600/روز
   🚶 متوسط — 6,500 تومان / 1,000 نفر · 🚀 2,500/روز
   ⚡️ سریع — 10,000 تومان / 1,000 نفر · 🚀 12,000/روز
   👥 از 2,000 تا 60,000

💬 برای سفارش دکمه برگشت را بزنید.

[◀️ برگشت به خرید]      ← برمی‌گردد به بخش ثبت سفارش
```

### ویرایش

`/panel` → **📋 لیست تعرفه‌ها**

| دکمه | کار |
|------|-----|
| ✏️ متن تعرفه | متن بالای جدول — ✨ ایموجی پریمیوم و نقل‌قول پشتیبانی می‌شود |
| 📊 جدول | جدول خودکار قیمت‌ها روشن/خاموش |
| 🔘 متن دکمه · 😀 ایموجی · 🎨 رنگ | ظاهر دکمه «لیست تعرفه‌ها» |
| ◀️ متن دکمه برگشت · 😀 ایموجی · 🎨 رنگ | ظاهر دکمه برگشت |
| 👁 پیش‌نمایش | همان چیزی که مشتری می‌بیند |

اگر در متن `{list}` بنویسید، جدول **دقیقا همان‌جا** می‌نشیند. وگرنه زیر متن اضافه می‌شود.

---

## 📞 پشتیبانی — دو دکمه

کاربر فقط **دو دکمه** می‌بیند:

```
┌──────────────────────────┐
│    💬 ارتباط مستقیم       │  → t.me/malakeBTC
├──────────────────────────┤
│  📨 ارتباط غیر مستقیم     │  → فهرست باز می‌شود
└──────────────────────────┘
```

پنل → **📞 پشتیبانی**

**دو دکمه اصلی:** ایموجی، متن، رنگ، ایموجی پریمیوم — و برای مستقیم، لینک مقصد.

**ارتباط غیر مستقیم:** دکمه‌ای ندارد — کاربر پیامش را می‌نویسد و مستقیم برای ادمین می‌رود.
متنش در ربات → 📝 متن‌ها → «💬 پیام ارتباط غیر مستقیم» قابل ویرایش است.

---

## 💠 دکمه شیشه‌ای زیر هر بخش

هر دکمه منو می‌تواند مجموعه‌ای **دکمه شیشه‌ای اختصاصی** زیر خودش داشته باشد —
با چیدمان و رنگ دلخواه.

```
🛒 خرید محصول            ← دکمه منو (بالا)

📦 محصولات
  💎 اشتراک طلایی — 250,000

👤 ممبر اخلاقی   🤖 ممبر فیک     ← دکمه‌های شیشه‌ای زیرش
      💎 ممبر ویژه               ← چیدمان 2,1
```

**ساختنش:** در ربات `/panel` → 🎨 دکمه‌ها → روی دکمه بزنید →
**💠 دکمه‌های شیشه‌ای زیرش** → ➕ افزودن

برای هر زیردکمه: ✏️ متن · 😀 ایموجی · 🎨 رنگ · ✨ پریمیوم · 📐 ردیف · 🔢 ترتیب · 🔀 نوع · 📝 مقدار

**چهار نوع رفتار:**

| نوع | کار |
|-----|-----|
| **متن** | یک متن نمایش می‌دهد |
| **لینک** | کاربر را به آدرسی می‌برد |
| **محصول** | مستقیم می‌رود سراغ یک محصول |
| **بخش** | یک بخش دیگر ربات را باز می‌کند |

**چیدمان:** 📐 چیدمان → `2,1` یعنی ۲ تا بالا، ۱ تا پایین. یا شماره ردیف هر زیردکمه را جدا بدهید.

---

---

## 🔒 قفل خودکار کانال بعد از پرداخت

**بله — خودکار انجام می‌شود.** به‌محض اینکه سفارش پرداخت و تایید شد:

```
مشتری پرداخت می‌کند
        ↓
سفارش تایید می‌شود (کیف پول = آنی، رسید = بعد از تایید شما)
        ↓
🎯 یک کمپین خودکار ساخته می‌شود
   کانال = کانال مشتری · هدف = تعداد سفارش · سقف روزانه = نفر/روز سرعت انتخابی
        ↓
🔒 کانال روی **۲ ربات اپلودر** قفل می‌شود — کم‌بارترین‌ها انتخاب می‌شوند
        ↓
هر کسی بخواهد از ربات اپلودر فایل بگیرد، اول باید عضو شود
        ↓
هر عضو تازه یک بار شمرده می‌شود (تکراری شمرده نمی‌شود)
        ↓
🔓 به‌محض رسیدن به تعداد سفارش، قفل خودکار برداشته می‌شود
```

هم شما و هم مشتری پیام می‌گیرید:

```
🎯 کمپین خودکار ساخته شد          ← به ادمین

📣 کانال: کانال مشتری
👥 هدف: 5,000 عضو
🚀 سقف روزانه: 8,000 نفر
🧾 سفارش: or_xxxx
```

مشتری پیشرفت را در **📊 پیگیری سفارش** می‌بیند:

```
✅ تایید شده
   🛒 غیراخلاقی
   📣 https://t.me/customerchan
   👥 5,000 نفر · ⚡️ سرعت بالا
   📊 ██████░░░░ 62%  (3,100/5,000)
```

### 🤖 پخش بین ربات‌ها — ۲ ربات، ۴ کانال

دو قانون سفت:

| قانون | مقدار |
|-------|-------|
| هر کانال روی چند ربات قفل شود | **۲ ربات** |
| هر کاربر حداکثر چند کانال ببیند | **۴ کانال** |

وقتی کمپین ساخته می‌شود، **کم‌بارترین ربات‌ها** انتخاب می‌شوند تا سفارش‌ها پخش شوند.
اگر کمپین‌های فعال از سقف بیشتر باشند، بقیه در نوبت می‌مانند تا جا باز شود
(قدیمی‌ترین سفارش‌ها اول تمام می‌شوند).

> **مهم:** قفل کمپین‌ها به تنظیم «عضویت اجباری» هر ربات ربطی ندارد — سفارش مشتری است
> و همیشه اجرا می‌شود. تنظیم آن ربات فقط روی **کانال‌های ثابت خودتان** اثر دارد.

نمای کامل: `/panel` → **🔒 قفل‌های عضویت اجباری**

```
🔒 قفل‌های عضویت اجباری

هر کانال روی 2 ربات قفل می‌شود، و هر کاربر حداکثر 4 کانال می‌بیند.

✅ @up1 — 3 کانال
   🔒 Niktobtc
   🔒 کانال 3
   🔒 کانال 5
✅ @up2 — 3 کانال
   …

[🔄 پخش دوباره بین ربات‌ها]
```

### 🚀 سقف روزانه — سرعت واقعا اثر می‌گذارد

سرعتی که مشتری خرید، سقف روزانه کمپین می‌شود. وقتی سهمیه امروز پر شد،
آن کانال **تا فردا** دیگر قفل نمی‌کند و نوبت به سفارش‌های دیگر می‌رسد.
پس «سرعت بالا» واقعا زودتر تحویل می‌شود، نه فقط گران‌تر.

### ⚠️ اگر ربات در کانال ادمین نباشد

بدون ادمین بودن ربات مادر، عضویت قابل شمارش نیست. دو حالت:

- **موقع سفارش** — مرحله «✅ ادمین کردم» جلوی این را می‌گیرد. لینک خصوصی مشکلی ندارد:
  به‌محض اینکه مشتری ربات را ادمین کند، تلگرام آیدی کانال را می‌فرستد و همه‌چیز خودکار پیش می‌رود.
  در حالت نادری که آیدی کانال گرفته نشده باشد، **سفارش گم نمی‌شود** — کمپینش ساخته می‌شود
  ولی **خاموش**، و به شما پیام می‌رسد. بعد از ادمین کردن ربات، در
  پنل → 🎯 سفارش ممبر آیدی کانال را در همان کمپین بگذارید و ذخیره کنید؛ خودش روشن می‌شود.
- **بعدا، اگر ربات را اخراج کنند** — بعد از ۳ بار خطای پشت‌سرهم، آن کمپین
  **موقتا متوقف می‌شود** و به شما پیام می‌رسد.
  اینطوری یک کانال خرابِ مشتری، کل ربات‌های اپلودر شما را قفل نمی‌کند.

### دستی هم می‌شود

پنل وب → 🎯 کمپین‌ها → «ثبت دستی سفارش ممبر» — برای سفارش‌هایی که خارج از ربات گرفته‌اید.
کمپین‌های خودکار با برچسب ⚡️ و شناسه سفارششان مشخص‌اند.

---

## 🤖 ربات‌های اپلودر

### چه کار می‌کنند؟
**فقط آپلود فایل و ساخت لینک.** عمومی هستند — هر کسی می‌تواند لینک را باز کند.

### جریان کامل

```
۱. شما داخل ربات اپلودر فایل می‌فرستید
        ↓
۲. ربات یک لینک می‌سازد:
   https://t.me/uploader_bot?start=Xk92mQ...
        ↓
۳. لینک را هرجا می‌خواهید پخش می‌کنید
        ↓
۴. کاربر روی لینک کلیک می‌کند
        ↓
۵. 🔒 اگر عضو کانال‌های اجباری نباشد:
   دکمه‌های کانال + «✅ عضو شدم» نشان داده می‌شود
        ↓
۶. بعد از عضویت → فایل ارسال می‌شود
   متن «۳۰ ثانیه» و دکمه‌ها **زیر خود فایل** می‌آیند، نه پیام جدا
        ↓
۷. ⏱ بعد از ۳۰ ثانیه فایل خودکار حذف می‌شود
```

### زیر فایل چه می‌آید؟

```
[عکس/فایل]
عکس نمونه

⚠️ این فایل تا 30 ثانیه دیگر حذف می‌شود.
لطفا آن را ذخیره یا فوروارد کنید.

[📥 تعداد دانلود]  [👁 بازدید]
```

دو دکمه آمار، عددشان را روی خود دکمه نشان می‌دهند — پیام جدیدی فرستاده نمی‌شود.
(دکمه «اطلاعات فایل» برداشته شد؛ اگر خواستید از پنل برش گردانید.)

### افزودن ربات
پنل → **🤖 ربات‌های اپلودر** → توکن را بزنید.
نام کاربری و وبهوک خودکار تنظیم می‌شود و تنظیمات پیش‌فرض روی آن سوار می‌گردد.

### آپلود
داخل خود ربات اپلودر (با حساب ادمین):
- **📤 آپلود تکی** — یک فایل → یک لینک
- **📦 آپلود گروهی** — چند فایل → یک لینک، در پایان `/done`
- **🔗 لینک‌های من** — همه لینک‌ها با آمار کلیک و تحویل
- **📊 آمار**

پشتیبانی از سند، عکس، ویدیو، صوت، ویس، گیف، ویدیو‌نوت و استیکر.

### تنظیمات کامل هر ربات
پنل → **🤖 ربات‌های اپلودر** — هر ربات یک بخش کامل جدا دارد:

**⚙️ رفتار:** زمان حذف، عضویت اجباری، محافظت فایل، انتظار درون‌خطی

**📢 کانال‌ها:** انتخاب کانال‌های مخصوص همین ربات

**🎨 دکمه‌های شیشه‌ای:** ایموجی، متن، رنگ واقعی، ایموجی پریمیوم، ردیف و ترتیب هر دکمه

**💠 رنگ دکمه‌های داخل ربات:** کانال · عضو شدم · بازگشت · انصراف · آپلود · اطلاعات

**📝 همه متن‌های ربات:** منو، شروع، عضویت اجباری، هشدار حذف، بعد از حذف، لینک نامعتبر
— با نوار ابزار نقل‌قول و ایموجی پریمیوم

**👑 مدیران:** برای هر ربات مدیر جدا ثبت کنید. مدیر می‌تواند فایل آپلود کند و لینک بسازد،
ولی به پنل وب و ربات مادر دسترسی ندارد. (آیدی عددی را با `/id` بگیرید.)

**📋 اعمال روی همه ربات‌ها:** تیک بزنید تا همین تنظیمات روی همه ربات‌ها کپی شود.

---

## 📢 کانال‌های عضویت اجباری

> ✅ **فقط ربات مادر باید در کانال ادمین باشد.**
> ربات‌های اپلودر لازم نیست عضو یا ادمین هیچ کانالی باشند —
> بررسی عضویت همیشه با توکن ربات مادر انجام می‌شود.

**ثبت خودکار:** کافی است ربات مادر را در کانالی ادمین کنید؛ کانال خودکار در پنل ثبت
می‌شود و پیام تاییدش برایتان می‌آید. (اگر ربات از کانال حذف شود هم خبر می‌دهید.)

**کانال مخصوص هر ربات:** در پنل → 🤖 ربات‌های اپلودر → برای هر ربات انتخاب کنید
کدام کانال‌ها برایش چک شود. اگر هیچ‌کدام را نزنید، کانال‌های «همه» اعمال می‌شود.

با دکمه **🩺 بررسی سلامت** مطمئن شوید ربات مادر به همه کانال‌ها دسترسی دارد.

> اگر بررسی ممکن نباشد، قفل **بسته** می‌ماند (fail-closed) تا کسی نتواند دورش بزند.

---

## 🛒 محصولات و پرداخت

پنل → **🛒 محصولات**

هر محصول: نام، قیمت، واحد (تومان/USDT/TRX)، محدودیت خرید، توضیح.

**اتصال محتوا:** محصول را به یک ربات اپلودر + کد لینک وصل کنید.
بعد از تایید خرید، لینک خودکار برای مشتری ارسال می‌شود.

### روش‌های پرداخت
- **کیف پول** — اگر موجودی کافی باشد، آنی و بدون دخالت ادمین
- **پرداخت مستقیم** — کاربر رسید می‌فرستد → ادمین تایید می‌کند

### تایید سفارش
از **تلگرام** (اعلان با دکمه 🟢 تایید / 🔴 رد) یا از **پنل → 🧾 سفارش‌ها**.
هر دو یک کار را می‌کنند و فقط ادمین دسترسی دارد.

---

## 👥 زیر مجموعه گیری

هر کاربر لینک اختصاصی `?start=ref<آیدی>` می‌گیرد.
بعد از هر خرید تاییدشده زیرمجموعه، درصد تعیین‌شده به کیف پول معرف واریز می‌شود.

درصد در پنل → **⚙️ تنظیمات** → «درصد پورسانت».

---

## 🔐 امنیت

| مورد | رفتار |
|------|--------|
| کاربر روی دکمه تایید ادمین بزند | ❌ رد — «دسترسی ندارید» |
| کاربر عادی بخواهد در ربات اپلودر آپلود کند | ❌ رد — فقط ادمین |
| کد لینک جعلی | ❌ رد |
| ربات در کانال ادمین نیست | ❌ قفل بسته می‌ماند + هشدار به ادمین |
| تایید دوباره یک سفارش | ❌ رد |
| خرید بعد از پر شدن ظرفیت | ❌ رد |
| کاربر معرف خودش شود | ❌ رد |
| فرم پنل بدون CSRF | ❌ خطای ۴۰۰ |
| رمز اشتباه پنل | ❌ رد (`hash_equals`) |

---

## 📂 ساختار داده

```
data_master/
├── config.json       دکمه‌ها، متن‌ها، پشتیبانی، کیف پول
├── products.json     محصولات + خریداران
├── orders.json       سفارش‌ها و شارژها
├── users.json        کاربران + موجودی + معرف
├── bots.json         ربات‌های اپلودر + تنظیمات هرکدام
├── channels.json     کانال‌های اجباری
├── delqueue.json     صف حذف خودکار
├── states.json       وضعیت گفتگوها
└── bots/<botID>/
    ├── links.json    لینک‌ها + فایل‌ها + آمار
    ├── users.json
    └── states.json
```

همه نوشتن‌ها **اتمیک** و **قفل‌دار** هستند.

---

## ⌨️ دستورها

**کاربر:** `/start` · `/menu` · `/id` · `/cancel`
**ادمین (ربات مادر):** `/panel`

### 📢 پیام همگانی

- **به کاربران ربات مادر** — پنل → 👥 کاربران → بالای صفحه
- **به کاربران ربات‌های زیرمجموعه** — پنل → 👥 کاربران → «پیام همگانی به ربات‌های زیرمجموعه».
  با توکن خود ربات اپلودر فرستاده می‌شود، پس به کسانی هم می‌رسد که فقط ربات فرعی را
  استارت کرده‌اند. می‌توانید ربات‌های مقصد را انتخاب کنید یا خالی بگذارید تا به همه برود.
**ادمین (ربات اپلودر):** `/panel`

---

## ❓ رفع اشکال

**ربات جواب نمی‌دهد** → پنل → داشبورد → «تنظیم وبهوک». دامنه باید HTTPS معتبر داشته باشد.

**کاربر می‌گوید عضو کانالم ولی فایل نمی‌گیرم** → پنل → 📢 کانال‌ها → 🩺 بررسی سلامت.
احتمالا ربات در کانال ادمین نیست.

**فایل بعد از ۳۰ ثانیه حذف نمی‌شود** → کران را تنظیم کنید (بخش نصب، مرحله ۴).

**ربات اپلودر جواب نمی‌دهد** → پنل → 🤖 ربات‌ها → «تنظیم دوباره وبهوک».

**ترافیک بالا دارم** → در تنظیمات هر ربات `inline_wait` را خاموش کنید و فقط به کران تکیه کنید.

---

## 🎯 سفارش ممبر دستی (کمپین)

> سفارش‌هایی که داخل ربات پرداخت می‌شوند **خودکار** کمپین می‌سازند —
> بخش [🔒 قفل خودکار کانال بعد از پرداخت](#-قفل-خودکار-کانال-بعد-از-پرداخت) را ببینید.
> این بخش برای سفارش‌های خارج از ربات است.

پنل → **🎯 سفارش ممبر**

مشتری N ممبر سفارش می‌دهد → کانالش در بخش **عضویت اجباری قفل** می‌شود →
هر کاربری که عضو شود شمرده می‌شود → به‌محض پر شدن سهمیه، کانال **خودکار از قفل خارج** می‌شود.

```
🎯 کانال مشتری — 247 / 1000
████████░░░░░░░░░░░░░░  24%
باقی‌مانده: 753
```

| تنظیم | کار |
|-------|-----|
| آیدی کانال مشتری | `@customer` یا `-100…` |
| تعداد ممبر سفارش | سهمیه |
| ربات‌های اپلودر | خالی = همه |
| ربات‌های شریک | خالی = همه |
| یادداشت | شماره فاکتور و … |

**قوانین شمارش:**
- هر کاربر برای هر کمپین **فقط یک بار** شمرده می‌شود
- شمارش فقط وقتی انجام می‌شود که کاربر **همه** شرط‌ها را رد کرده باشد
- بعد از پر شدن سهمیه، دیگر چیزی شمرده نمی‌شود
- ⚠️ ربات مادر باید در کانال مشتری **ادمین** باشد

---

## 🤝 ربات‌های شریک (سورس مستقل)

پنل → **🤝 ربات‌های شریک**

برای کسی که **ربات خودش با سورس خودش** را دارد و می‌خواهد زیرمجموعه شما شود.
توکن رباتش را نمی‌گیرید — فقط یک **کلید API** می‌دهید که با آن بپرسد
«این کاربر باید عضو کدام کانال‌ها شود؟».

### آدرس‌ها

```
POST https://دامنه/bot_master_membership.php?api=check
     key=pk_xxxxx & user_id=123456

POST https://دامنه/bot_master_membership.php?api=channels
     key=pk_xxxxx
```

### پاسخ

```json
{
  "ok": true,
  "allowed": false,
  "missing": [{"title":"کانال مشتری","url":"https://t.me/..."}],
  "message": "برای ادامه، در کانال‌های زیر عضو شوید."
}
```

ربات شریک فقط `allowed` را نگاه می‌کند: اگر `false` بود دکمه‌های `missing` را
نشان می‌دهد، اگر `true` بود محتوا را می‌فرستد.

**کد آماده PHP و Python** داخل پنل، زیر هر شریک، آماده کپی است.

### امنیت

| مورد | رفتار |
|------|--------|
| کلید اشتباه یا خالی | ❌ HTTP 401 |
| شریک غیرفعال | ❌ HTTP 403 |
| بیش از ۶۰۰ درخواست در دقیقه | ❌ HTTP 429 |
| کانال قابل بررسی نباشد | ❌ `allowed:false` (fail-closed) |
| تعویض کلید | کلید قبلی فوراً باطل می‌شود |

> شریک هرگز به توکن ربات‌ها، داده کاربران یا پنل دسترسی ندارد —
> فقط جواب بله/خیر و فهرست کانال‌ها.

> در کد نمونه، اگر سرویس در دسترس نبود `allowed:false` برمی‌گردد.
> این عمدی است: قفل نباید با قطعی شبکه باز شود.

---

## 🤖 تنظیمات ربات‌های زیرمجموعه (در ربات)

`/panel` → **🤖 تنظیمات ربات‌های زیرمجموعه**

اینجا **پیش‌فرض** همه ربات‌های اپلودر را تعیین می‌کنید. هر ربات **جدیدی** که اضافه شود
همین تنظیمات را می‌گیرد؛ برای ربات‌های موجود دکمه **📋 اعمال روی همه ربات‌ها** را بزنید.

- ⏱ زمان حذف · 🔒 عضویت اجباری · 🛡 محافظت فایل
- **📝 متن‌های ربات زیرمجموعه** — هر ۸ متن با **ایموجی پریمیوم**، **نقل‌قول** و بازگردانی پیش‌فرض:
  منوی پنل · پیام شروع · عضویت اجباری · دکمه «عضو شدم» · هشدار حذف ·
  بعد از حذف · لینک نامعتبر · اطلاعات فایل

---

## 📊 دکمه زیر فایل + آمار

زیر هر فایلی که تحویل داده می‌شود یک **دکمه شیشه‌ای** می‌آید (پیش‌فرض: 📊 اطلاعات فایل):

```
📊 اطلاعات فایل

📦 فیلم آموزشی
📁 تعداد فایل: 1
👁 بازدید: 2
📥 دانلود: 1
📅 2026-08-16 20:27
```

در **📊 آمار** ربات اپلودر هم بازدید کل، دانلود کل و **پربازدیدترین لینک‌ها** نشان داده می‌شود.

---

## 🎨 رنگ دکمه‌های ثابت

📝 متن‌ها → 🔤 متن دکمه‌های ثابت → کنار هر دکمه یک **🎨** هست.
با هر ضربه رنگ عوض می‌شود: بی‌رنگ → 🔵 آبی → 🟢 سبز → 🔴 قرمز.
رنگ اختصاصی بر رنگ نقش اولویت دارد.

---

## 🧹 ضد شلوغی — جایگزینی پیام

ربات به‌جای فرستادن پیام جدید، **همان پیام قبلی را عوض می‌کند**. دو اسلات جدا:

| اسلات | کدام بخش‌ها |
|-------|-------------|
| **menu** | حساب کاربری · پیگیری سفارش · پشتیبانی · زیرمجموعه · اعتماد |
| **shop** | خرید محصول و **کل جریان سفارش** |
| **wallet** | افزایش موجودی و پرداختش |

بخش‌های منو با هم جایگزین می‌شوند و شلوغ نمی‌کنند.
بخش محصول **پیام خودش را دارد** — وقتی روی دکمه محصول می‌زنید، محتوای تازه
جای همان پیام محصول می‌نشیند و به پیام‌های منو کاری ندارد.

جریان سفارش (لینک → تعداد → نرخ → سرعت → فاکتور → پرداخت) هم همه در **یک پیام** جلو می‌رود.

### ↩️ ریپلای روی دکمه

وقتی کاربر یک دکمه منو را می‌زند، جواب ربات **ریپلای همان پیام** می‌شود —
پس معلوم است جواب کدام دکمه است. پیام قبلی همان اسلات هم حذف می‌شود تا شلوغ نشود.

با `/start` هر سه اسلات تازه می‌شوند.

> اگر پیام قدیمی پاک شده یا خیلی قدیمی باشد، ربات خودکار پیام تازه می‌فرستد
> و از آن به بعد همان را عوض می‌کند.

---

## ⚡️ سرعت واقعاً روی سفارش اثر می‌گذارد

هر سرعت دو چیز دارد: **ضریب قیمت** و **سرعت ممبرگیری روزانه**.

| سرعت | ضریب قیمت | ممبر در روز | ۵۰۰۰ نفر چقدر طول می‌کشد |
|------|-----------|-------------|--------------------------|
| 🐢 کند | ×1 | 500 | حدود ۱۰ روز |
| 🚶 متوسط | ×1.3 | 2,000 | حدود ۳ روز |
| ⚡️ سرعت بالا | ×1.7 | 8,000 | کمتر از یک روز |

روی دکمه هم نوشته می‌شود: `⚡️ سرعت بالا — 8,000/روز`

**در فاکتور:**
```
👥 تعداد کاربر درخواستی: 5,000 نفر
⚡️ سرعت: ⚡️ سرعت بالا
🚀 سرعت ممبرگیری: 8,000 نفر در روز
⏳ زمان تقریبی تکمیل: کمتر از یک روز
💳 مبلغ قابل پرداخت: 2,125,000 تومان
```

این اطلاعات روی خود سفارش ذخیره می‌شود و در **پیگیری سفارش** و **اعلان ادمین** هم دیده می‌شود.

تنظیمش: `/panel` → 🛒 محصولات → محصول → ⚡️ سرعت‌ها → روی هر سرعت.

---

## 🛒 وصل کردن دکمه به محصول

> **معمولا خودکار انجام می‌شود.** اگر نام دکمه با نام یک محصول یکی باشد،
> ربات همان اولین کلیک خودش وصلش می‌کند و مستقیم می‌رود سراغ جریان سفارش.
> پس کافی است محصولی با همان نام دکمه بسازید.

اگر نام‌ها یکی نیستند، دو راه دارید:

**راه سریع:** `/panel` → 🎨 دکمه‌ها → دکمه مادر → 💠 دکمه‌های شیشه‌ای زیرش →
**🔗 وصل خودکار به محصولات هم‌نام** — همه را یکجا وصل می‌کند و می‌گوید کدام‌ها جا ماندند.

**راه دستی:**

**درستش:** `/panel` → 🎨 دکمه‌ها → دکمه مادر → 💠 دکمه‌های شیشه‌ای زیرش →
روی زیردکمه → **🔀 نوع** را تا «محصول» بچرخانید → **🛒 انتخاب محصول** → محصول را بزنید.

از آن به بعد با زدن آن دکمه، اگر محصول **جریان سفارش** داشته باشد (نشانه 🔄)
یک‌راست می‌رود سراغ گرفتن لینک کانال ← تعداد ← سرعت ← فاکتور.

> موقع ساختن دکمه جدید هم ربات خودش دکمه **«🛒 وصل کردن به یک محصول»** را نشان می‌دهد.
