# 🛍️💎 سیستم فروش ممبرشیپ — راهنمای کامل

سیستم کامل فروش عضویت تلگرام: ربات مادر می‌فروشد، ربات‌های فرعی فایل تحویل می‌دهند،
پنل وب همه چیز را مدیریت می‌کند.

---

## 📦 فایل‌ها

| فایل | نقش |
|------|------|
| `bot_master_membership.php` | ربات مادر + ربات‌های فرعی + همه منطق داده |
| `admin_panel.php` | پنل مدیریت وب |
| `data_master/` | داده‌ها (خودکار ساخته می‌شود) |

فقط همین دو فایل PHP لازم است. نه دیتابیس، نه کتابخانه بیرونی.

---

## ⚙️ نصب در ۵ دقیقه

### ۱. آپلود
هر دو فایل را کنار هم روی هاست PHP (نسخه ۸.۰ به بالا) بگذارید.

### ۲. تنظیم توکن و ادمین
در `bot_master_membership.php` خطوط ۲۲–۲۳:

```php
if (!defined('BOT_TOKEN')) define('BOT_TOKEN', 'توکن_ربات_مادر');
if (!defined('ADMIN_ID'))  define('ADMIN_ID',  8213021584);
```

آیدی عددی خود را با فرستادن `/id` به ربات می‌گیرید.

### ۳. تنظیم رمز پنل
در `admin_panel.php` خط ۱۰:

```php
define('ADMIN_PASSWORD', 'یک_رمز_قوی_بگذارید');
```

> ⚠️ رمز پیش‌فرض `admin123456` است — حتما عوضش کنید.

### ۴. باز کردن پنل و تنظیم وبهوک
به `https://دامنه‌شما/admin_panel.php` بروید، وارد شوید،
در تب **داشبورد** دکمه «تنظیم وبهوک ربات مادر» را بزنید. تمام.

### ۵. محافظت از پوشه داده
پوشه `data_master/` شامل توکن ربات‌هاست. در `.htaccess` کنار آن بگذارید:

```apache
Deny from all
```

---

## 🔄 جریان کامل خرید

```
کاربر /start
   ↓
🟢 خرید «پریمیوم — 50 USDT»
   ↓
ربات آدرس کیف پول و شناسه سفارش می‌دهد
   ↓
کاربر واریز می‌کند
   ↓
🟢 «پرداخت کردم، ارسال رسید»
   ↓
کاربر عکس رسید یا TxID می‌فرستد
   ↓
📨 ادمین اعلان می‌گیرد: [🟢 تایید] [🔴 رد]
   ↓
ادمین تایید می‌کند  ← فقط ادمین می‌تواند
   ↓
✅ کاربر عضو می‌شود + لینک اختصاصی می‌گیرد
   ↓
کاربر لینک را باز می‌کند → ربات فرعی فایل‌ها را می‌دهد
```

ادمین می‌تواند از **تلگرام** یا از **پنل وب** تایید کند — هر دو یک کار را می‌کنند.

---

## 🔐 امنیت — چه چیزهایی چک می‌شود

| مورد | رفتار |
|------|--------|
| کاربر روی دکمه تایید ادمین بزند | ❌ رد می‌شود — «دسترسی ندارید» |
| کاربر لینک دسترسی خود را به دیگری بدهد | ❌ رد می‌شود — لینک به آیدی گره خورده |
| کد دسترسی جعلی | ❌ رد می‌شود |
| عضو حذف‌شده با لینک قدیمی | ❌ رد می‌شود |
| تایید دوباره یک پرداخت | ❌ رد می‌شود |
| تایید بعد از پر شدن ظرفیت | ❌ رد می‌شود |
| عضو عادی بخواهد فایل آپلود کند | ❌ رد می‌شود — فقط مالک |
| فرم پنل بدون توکن CSRF | ❌ خطای ۴۰۰ |
| رمز اشتباه در پنل | ❌ رد می‌شود |

---

## 🌐 پنل وب

### تب‌ها

**📊 داشبورد** — کاربران، ممبرشیپ‌ها، اعضای فعال، ربات‌ها، منتظر تایید، فروش موفق،
درآمد تفکیک‌شده به ازای هر واحد پول، و دکمه تنظیم وبهوک.

**🧾 پرداخت‌ها** — لیست منتظر تایید با دکمه‌های ✅ تایید / ❌ رد.
تایید = عضو شدن خودکار + ارسال لینک به کاربر در تلگرام.
پایین‌تر تاریخچه ۱۰۰ پرداخت آخر.

**🛍️ ممبرشیپ‌ها** — ساخت ممبرشیپ (نام، قیمت، واحد، محدودیت، توضیح)،
فعال/غیرفعال کردن، حذف، و دیدن اعضا با امکان حذف تک‌تک
(حذف عضو، لینک دسترسی او را هم باطل می‌کند).

**🤖 ربات‌ها** — افزودن ربات با توکن (نام کاربری و وبهوک خودکار تنظیم می‌شود)،
تغییر ممبرشیپ متصل با یک منوی کشویی، تنظیم دوباره وبهوک، حذف.

**👥 کاربران** — لیست کاربران با عضویت‌هایشان، مسدود/رفع مسدودی، پیام همگانی.

**⚙️ تنظیمات** — آدرس کیف پول USDT و TRX، شماره کارت، یوزرنیم پشتیبانی،
دامنه، و متن خوش‌آمدگویی.

---

## 🤖 دستورهای ربات

### کاربر
| دستور | کار |
|-------|-----|
| `/start` | فروشگاه ممبرشیپ |
| `/id` | آیدی عددی خودت |
| `/cancel` | لغو عملیات جاری |

### ادمین
| دستور | کار |
|-------|-----|
| `/panel` | پنل مدیریت داخل تلگرام |

از پنل تلگرام می‌توانید ممبرشیپ بسازید (ویزارد چهار مرحله‌ای)،
ربات اضافه کنید، پرداخت‌ها را تایید کنید، پیام همگانی بفرستید و کیف پول‌ها را عوض کنید.

---

## 📤 ربات فرعی (اپلودر)

بعد از افزودن ربات در پنل، وبهوکش خودکار روی
`bot_master_membership.php?bot=<ID>` تنظیم می‌شود.

### مالک (همان ADMIN_ID)
- 🟢 **آپلود فایل** — یک فایل بفرستید، ذخیره می‌شود
- 🟢 **آپلود گروهی** — چند فایل پشت سر هم، در پایان `/done`
- 🔵 **آمار** — کاربران، فایل‌ها، دانلودها، اعضای ممبرشیپ
- 🔵 **پیام همگانی** — به همه کاربران آن ربات

پشتیبانی از سند، عکس، ویدیو، صوت، ویس و گیف.

### اعضا
فقط 🔵 **فایل‌های من** را می‌بینند و با زدنش همه فایل‌ها برایشان ارسال می‌شود.
غیر اعضا هیچ چیز نمی‌بینند.

---

## 🎨 دکمه‌های رنگی

| رنگ | کاربرد | نمونه |
|-----|--------|-------|
| 🟢 سبز | اقدام مثبت | خرید، تایید، آپلود |
| 🔴 قرمز | اقدام منفی | انصراف، رد، حذف |
| 🔵 آبی | منو و پیمایش | پنل، آمار، بازگشت |

---

## 📂 ساختار داده

```
data_master/
├── memberships.json   ممبرشیپ‌ها + لیست اعضا
├── bots.json          ربات‌های فرعی + توکن‌ها
├── payments.json      پرداخت‌ها + رسیدها + وضعیت
├── access.json        کدهای دسترسی (کد → کاربر → ممبرشیپ)
├── users.json         کاربران ربات مادر
├── settings.json      کیف پول‌ها و متن‌ها
├── states.json        وضعیت گفتگوها
└── bots/<botID>/
    ├── files.json     فایل‌ها
    ├── batches.json   مجموعه‌ها
    ├── users.json     کاربران آن ربات
    └── states.json    وضعیت گفتگوها
```

همه نوشتن‌ها **اتمیک** و **قفل‌دار** هستند: دو خرید همزمان همدیگر را پاک نمی‌کنند.

---

## 💡 مثال عملی

```
۱. پنل → ⚙️ تنظیمات → آدرس USDT خود را بگذارید
۲. پنل → 🛍️ ممبرشیپ‌ها → «دوره پایتون» / 50 / USDT / محدودیت 100
۳. @BotFather → ربات جدید بسازید → توکن بگیرید
۴. پنل → 🤖 ربات‌ها → توکن را بزنید، به «دوره پایتون» وصلش کنید
۵. ربات فرعی را /start بزنید (با حساب ادمین) → 🟢 آپلود گروهی → فایل‌ها → /done
۶. تمام. حالا مشتری‌ها می‌توانند بخرند.
```

---

## ❓ رفع اشکال

**ربات جواب نمی‌دهد**
پنل → داشبورد → «تنظیم وبهوک ربات مادر». دامنه باید HTTPS معتبر داشته باشد.

**«ربات اپلودری هنوز وصل نشده»**
پنل → 🤖 ربات‌ها → از منوی کشویی ممبرشیپ را انتخاب کنید.

**ربات فرعی جواب نمی‌دهد**
پنل → 🤖 ربات‌ها → «تنظیم وبهوک» را برای همان ربات بزنید.

**داده ذخیره نمی‌شود**
`chmod -R 755 data_master/` و مطمئن شوید PHP اجازه نوشتن دارد.

**رمز پنل را فراموش کردم**
خط ۱۰ فایل `admin_panel.php` را باز کنید و ببینید/عوض کنید.
