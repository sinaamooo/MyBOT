# 🤖 ربات‌ساز متحد - راهنمای استفاده

## 📋 فهرست مطالب

1. [نمای کلی](#نمای-کلی)
2. [تنظیمات اولیه](#تنظیمات-اولیه)
3. [کاربری ربات مادر](#کاربری-ربات-مادر)
4. [مدیریت ربات‌های فرعی](#مدیریت-ربات‌های-فرعی)
5. [سیستم فایل‌های آپلودی](#سیستم-فایل‌های-آپلودی)
6. [مدیریت کاربران](#مدیریت-کاربران)
7. [تنظیمات پیشرفته](#تنظیمات-پیشرفته)

## 🎯 نمای کلی

**bot_unified.php** یک سیستم جامع است که شامل:

- **ربات مادر** (Master Bot): مدیریت ایجاد ربات‌های فرعی، سکه‌ها، و پنل مدیریت
- **ربات‌های فرعی** (Child Bots): هر یک یک آپلودر فایل مستقل است
- **سیستم فایل‌ها**: آپلود تکی، آپلود گروهی، دریافت فایل
- **سیستم کاربری**: VIP، ادمین، کاربران مسدود شده
- **الزامیات**: عضویت اجباری در کانال‌ها، انجام وظایف

### دیتا ذخیره‌سازی

تمام اطلاعات **بدون دیتابیس** در فایل‌های JSON ذخیره می‌شوند:

```
data/
├── users.json              # کاربران ربات مادر
├── child_bots.json         # لیست ربات‌های فرعی
├── states.json             # وضعیت کاربران در ربات مادر
├── child_states.json       # وضعیت کاربران در ربات‌های فرعی
└── child_bots/
    ├── 1/
    │   ├── settings.json   # تنظیمات ربات
    │   ├── files.json      # فایل‌های آپلود شده
    │   ├── batches.json    # مجموعه‌های فایل
    │   ├── users.json      # کاربران ربات
    │   ├── vips.json       # کاربران VIP
    │   ├── admins.json     # ادمین‌های ربات
    │   ├── banned.json     # کاربران مسدود
    │   └── joins.json      # کانال‌های الزامی
    └── 2/
        └── (مشابه ربات 1)
```

## 🔧 تنظیمات اولیه

### 1. تنظیم توکن‌ها

باز کنید `bot_unified.php` و این خطوط را ویرایش کنید:

```php
define('BOT_TOKEN', 'YOUR_MASTER_BOT_TOKEN'); // توکن ربات مادر
define('ADMIN_ID', 8213021584);               // آیدی مدیر
define('BUILD_COST', 50);                      // هزینه ساخت ربات (سکه)
```

### 2. راه‌اندازی Webhook

دستور زیر را درخط فرمان اجرا کنید:

```bash
curl -X POST https://api.telegram.org/bot[YOUR_TOKEN]/setWebhook \
  -d "url=https://yourdomain.com/bot_unified.php"
```

جایگزین کنید:
- `[YOUR_TOKEN]` = توکن ربات مادر
- `yourdomain.com` = دامنه سایت شما

### 3. صادقانه سازی دسترسی

پوشه `data` باید قابل نوشتن باشد:

```bash
chmod -R 755 data/
```

## 👥 کاربری ربات مادر

### شروع کردن

کاربران `/start` را فشار دهند:

```
🤖 خوش آمدید به ربات‌ساز!
از این ربات می‌تونید ربات فرعی برای آپلودر فایل‌ها بسازید.
موجودی شما: 💰 0 سکه
هزینه ساخت هر ربات: 50 سکه

[✅ ساخت ربات جدید]
[💰 موجودی من] [🤖 ربات‌های من]
[ℹ️ راهنما] [🔐 پنل مدیریت] (فقط مدیر)
```

### شارژ سکه (برای مدیر)

مدیر می‌تواند در پنل مدیریت سکه‌های کاربران را شارژ/کسر کند.

### ساخت ربات جدید

1. روی `✅ ساخت ربات جدید` بزنید
2. کاربر را از @BotFather برای گرفتن توکن هدایت کنید
3. توکن را وارد کنید
4. 50 سکه کسر می‌شود
5. ربات فعال می‌شود

## 🤖 مدیریت ربات‌های فرعی

### وارد شدن به پنل ربات

صاحب ربات می‌تواند `/panel` را بزند یا از دکمه‌های منو استفاده کند:

```
⚙️ پنل مدیریت ربات

[📤 آپلود فایل]
[👥 آپلود گروهی]
[📣 پیام همگانی]
[📁 فایل‌ها و آمار]
[⚙️ تنظیمات]
[✖️ روشن/خاموش]
[🗑️ حذف ربات]
```

## 📤 سیستم فایل‌های آپلودی

### آپلود تکی

1. روی `📤 آپلود فایل` بزنید
2. فایل/عکس/ویدیو را ارسال کنید
3. سیستم یک **لینک اختصاصی** می‌دهد:
   ```
   https://t.me/your_bot?start=abc12345
   ```
4. هر کاربری که این لینک را باز کند، فایل را دریافت می‌کند

### آپلود گروهی

1. روی `👥 آپلود گروهی` بزنید
2. فایل‌های خود را یکی پس از دیگری ارسال کنید
3. `/done` یا `پایان` را بنویسید
4. سیستم یک **لینک یکپارچه** می‌دهد:
   ```
   https://t.me/your_bot?start=b_batch123
   ```
5. کاربر یک بار کلیک می‌کند، تمام فایل‌ها ارسال می‌شوند

### فروش فایل با الزامیات

اگر کانال‌ها یا وظایف‌های الزامی تنظیم شده‌اند:

1. کاربر لینک را باز می‌کند
2. الزام‌ها را انجام می‌دهد
3. سپس فایل دریافت می‌کند

## 👥 مدیریت کاربران

### VIP کاربران

VIP کاربران **نیاز ندارند**:
- عضویت الزامی در کانال‌ها
- انجام وظایف الزامی

اضافه کردن VIP:
```
⚙️ تنظیمات → 💎 کاربران VIP → [➕ افزودن VIP]
آیدی عددی کاربر را وارد کنید
```

### ادمین‌های ربات

فقط **صاحب ربات** می‌تواند ادمین‌ها را مدیریت کند:
```
⚙️ تنظیمات → 👮 ادمین‌ها → [➕ افزودن ادمین]
آیدی عددی ادمین را وارد کنید
```

### مسدود کردن کاربران

```
⚙️ تنظیمات → 👥 لیست کاربران
[🚫 مسدود کردن USER_ID]
```

کاربران مسدود شده دسترسی ندارند.

## ⚙️ تنظیمات پیشرفته

### تنظیمات ربات فرعی

```
⚙️ تنظیمات:
[🎯 وظیفه اجباری]      ← انجام کار قبل دریافت
[🔐 عضویت اجباری]      ← عضویت کانال‌ها
[🗂️ تنظیمات فایل‌ها]    ← محافظت، کپشن
[💎 کاربران VIP]        ← بدون الزامیات
[👮 ادمین‌ها]          ← مدیریت
[✏️ متن‌های ربات]       ← پیام خوش‌آمد، وضعیت
[✨ ایموجی پریمیوم]    ← شخصی‌سازی
[🗑️ حذف ربات]         ← حذف دائمی
```

### 1. وظیفه اجباری

کاربران باید **یک کار انجام دهند** تا فایل را بگیرند:

مثال: "عضو کانال تلگرام ما شوید" یا "لایک بدید"

**فعال کردن:**
```
🎯 وظیفه اجباری → [✅ فعال کردن]
✏️ متن وظیفه را بنویسید
```

### 2. عضویت اجباری

کاربران باید **عضو این کانال‌ها** شوند:

```
🔐 عضویت اجباری → [➕ افزودن کانال]
آیدی کانال: @your_channel
```

**نکته:** ربات باید ادمین آن کانال باشد.

### 3. تنظیمات فایل‌ها

```
🗂️ تنظیمات فایل‌ها:

[🔒 محافظت فایل]      ← جلوگیری از فوروارد/ذخیره
[✏️ کپشن فایل‌ها]     ← متن موقتی فایل (از {name} استفاده کنید)
```

### 4. متن‌های سفارشی

```
✏️ تغییر متن‌های ربات:

[💬 متن خوش‌آمد]      ← پیام شروع
[🔐 متن عضویت]        ← درخواست عضویت
[🎯 متن وظیفه]        ← درخواست وظیفه
[🗂️ کپشن فایل‌ها]     ← متن فایل (از {name} استفاده کنید)
```

## 📊 آمار و گزارش‌ها

### مشاهده آمار ربات

```
📁 فایل‌ها و آمار:

👥 کاربران: 145
💎 VIP: 5
🚫 مسدود: 2
📁 فایل‌ها: 23
```

### پنل مدیریت (مدیر فقط)

```
👑 پنل مدیریت پلتفرم:

[✅ آمار کلی]        ← تعداد کل کاربران، ربات‌ها
[💰 مدیریت سکه]      ← شارژ/کسر سکه
[🤖 مدیریت ربات‌ها]   ← فعال‌سازی/غیرفعال‌سازی
[📣 پیام همگانی]     ← پیام برای کاربران
```

## 📤 ارسال پیام همگانی

صاحب ربات می‌تواند **یک پیام برای تمام کاربران** ارسال کند:

```
📣 ارسال پیام همگانی:

متن پیام را وارد کنید...

نتیجه:
✅ موفق: 143
❌ ناموفق: 2
```

نرخ‌محدودی برای جلوگیری از محدودیت تلگرام اعمال می‌شود (50ms بین پیام‌ها).

## 🔐 ایموجی و شخصی‌سازی

### تغییر ایموجی پریمیوم

```
✨ ایموجی پریمیوم:

ایموجی فعلی: ✨
ایموجی جدید را بنویسید...
```

این ایموجی پیش‌وند پیام خوش‌آمد می‌شود.

## 🛡️ نکات امنیتی

1. **توکن‌ها:** توکن‌ها در `data/child_bots.json` ذخیره می‌شوند - از دسترسی عمومی محافظت کنید!

   ```bash
   chmod 600 data/child_bots.json
   ```

2. **دسترسی مدیریت:** فقط کاربران خاص (ADMIN_ID) می‌توانند:
   - سکه‌ها را مدیریت کنند
   - ربات‌ها را غیرفعال کنند
   - پیام همگانی ارسال کنند

3. **محافظت فایل:** صاحب ربات می‌تواند فایل‌ها را محافظت کند:
   - جلوگیری از فوروارد
   - جلوگیری از ذخیره‌سازی

4. **حدود نرخ:** سیستم خودکار نرخ‌محدودی برای broadcast اعمال می‌کند.

## 📱 مثال عملی

### سناریو: فروش دوره آموزشی

1. **ربات را ایجاد کنید:**
   - کاربر `/start` می‌کند
   - `✅ ساخت ربات جدید` را می‌زند
   - توکن از @BotFather را ارسال می‌کند
   - ربات فعال می‌شود

2. **فایل‌ها را آپلود کنید:**
   - `/panel` می‌کند
   - `👥 آپلود گروهی` را می‌زند
   - ویدیوهای درس را یکی پس از دیگری ارسال می‌کند
   - `/done` می‌کند
   - لینک گروهی دریافت می‌کند

3. **الزامیات تنظیم کنید:**
   - `⚙️ تنظیمات` می‌کند
   - `🔐 عضویت اجباری` اضافه می‌کند (`@my_course`)
   - `💎 کاربران VIP` برای خود اضافه می‌کند (بدون الزام)

4. **فروش را شروع کنید:**
   - لینک گروهی را به مردم می‌دهد
   - کاربران عضویت می‌کنند
   - ویدیوها دریافت می‌کنند

## 🐛 عیب‌یابی

### مشکل: "توکن نامعتبر است"

- ✓ توکن را درست کپی کردید؟
- ✓ توکن صحیح است (از @BotFather گرفته‌اید)؟
- ✓ اتصال اینترنت خوب است؟

### مشکل: "موجودی کافی نیست"

- ✓ کاربر باید 50 سکه داشته باشد
- ✓ مدیر می‌تواند سکه شارژ کند (`💰 مدیریت سکه`)

### مشکل: "فایل پیدا نشد"

- ✓ آیا فایل صحیح آپلود شد؟
- ✓ آیا لینک درست است؟
- ✓ پوشه `data` قابل نوشتن است؟

### مشکل: "عضویت اجباری کار نمی‌کند"

- ✓ ربات ادمین کانال است؟
- ✓ آیدی کانال درست است (`@channel` یا `-100123456789`)?
- ✓ کاربر واقعاً عضو شده است؟

## 📞 پشتیبانی

برای مسائل فنی:
1. پوشه `data` و فایل‌های JSON را بررسی کنید
2. لاگ‌های PHP را چک کنید
3. Webhook دوباره راه‌اندازی کنید

## 📝 نسخه‌ها

- **v1.0**: نسخه اول - تمام فیچرها شامل است
  - ربات‌ساز متحد
  - سیستم فایل‌ها
  - مدیریت کاربران
  - تنظیمات سفارشی

---

**فایل اصلی:** `bot_unified.php`

**تاریخ ایجاد:** 2024

**لایسنس:** استفاده آزاد برای مقاصد شخصی و تجاری
